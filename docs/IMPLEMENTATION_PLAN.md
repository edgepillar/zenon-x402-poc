# Implementation plan: first-class Zenon support in x402

## Scope

The current prototype explores:

- x402 v2;
- scheme `exact`;
- an experimental Zenon network family;
- concrete ZTS transfers represented by a payer-signed, unpublished `UserSend` account block;
- a self-hosted facilitator that never receives payer private keys.

No go-zenon consensus change or hypothetical sequence-independent authorization primitive is assumed.

## Current v0.2 payload and requirement

The mechanism payload is:

```ts
type ExactZenonPayloadV2 = {
  transaction: ZenonAccountBlockJson;
  intentDigest: string;
};
```

The selected requirement uses the ordinary x402 fields plus an exact chain profile:

```json
{
  "scheme": "exact",
  "network": "zenon:testnet",
  "asset": "zts1...",
  "amount": "<canonical-positive-atomic-integer>",
  "payTo": "z1...",
  "maxTimeoutSeconds": 60,
  "extra": {
    "poc": true,
    "paymentFlow": "upfront",
    "settlement": "account-block",
    "zenonChain": {
      "version": 1,
      "chainIdentifier": "<canonical-nonzero-decimal-string>",
      "genesisMomentumHash": "<64-lowercase-hex>"
    }
  }
}
```

The CLI supplies one exact, internally pinned historical testnet profile under an explicit operator-trust policy. `zenon:testnet` remains descriptive, not authenticated chain identity and not a claim of registered CAIP-2 naming. The policy checks a pinned five-field height-2 identity tuple before signing or publication, but it does not authenticate the RPC endpoint, verify the remaining Momentum fields, or prove linkage to the observed frontier. Equal chain identifiers alone still do not distinguish chains with different genesis Momentums.

The amount range is `1` through `2^255 - 1` atomic units. This is narrower than the account-block hash's 32-byte amount field because canonical go-zenon validation rejects values with a bit length above 255.

The intent digest covers the complete selected requirement and resource. Before standardization, generic JSON canonicalization should be replaced with a specified, domain-separated encoding and portable conformance vectors.

### Implemented local offer-selection boundary

The buyer applies generic stable-v2 structural validation to every advertised offer before selection. Built-in mock and live clients expose deeply immutable, client-owned `paymentCapabilities` descriptors for their exact scheme, local Zenon network label, and `upfront` flow. Multi-offer selection preserves order, skips structurally valid unsupported alternatives, and selects the first route matching the client's x402 version, scheme, network, and exact `upfront` flow. A claimed supported route receives complete strict Zenon validation; failure invalidates the challenge before payment construction.

A descriptor-less client retains existing single-offer behavior and rejects multi-offer challenges as ambiguous. Wrappers must explicitly copy the immutable descriptor to support multiple offers. Selection performs no speculative signing, SDK initialization, RPC, PoW, settlement, or protected-resource delivery. For both single-offer and multi-offer challenges, payment construction receives a detached view containing only the selected offer. Success and recovery retain the untouched original challenge, so mutation of the client-owned view cannot change the authoritative resource or selected requirement. The missing-flow option is restricted to the existing single-offer Phase 2A characterization lane.

This is a narrow internal compatibility boundary, not a replacement for official registered scheme handlers and not a claim of complete stable-v2 compatibility, official x402 or Zenon network registration, Phase 2C activation, hardware-wallet support, or production readiness.

## Implemented live-safety foundation

### Offline preflight

`verify()` and `settle()` call the same strict offline preflight before RPC. Direct settlement therefore cannot bypass envelope, requirement, profile, HTTPS live-resource, block, hash, signature, payer, recipient, asset or amount validation.

### Two ordering layers

```text
canonical payer queue
    |
    v
process-wide SDK owner
    |
    v
configure -> connect -> RPC/prepare -> close -> release
```

The payer queue orders logically competing settlements within one facilitator instance. The global owner protects the TypeScript SDK's mutable singleton across all buyer and facilitator instances. The enforced lock order is payer queue before global SDK owner. Client preparation and standalone verification acquire only the global owner.

This serializes one Node.js process, not other processes, facilitators or publishers.

### Fail-closed timeouts

Simple reads and publication have bounded local waits. Because SDK requests are not cancellable, a timeout poisons the process-wide live runtime before ownership release. Teardown is best effort, queued and future owners fail, and the process must restart. An unexpected cleanup failure also poisons the runtime before release. The code does not claim the late request was cancelled.

After `publish()` returns its asynchronous request promise, every promise rejection is reconciled by exact transaction lookup. If the exact block is not observed, the result remains `SUBMISSION_OUTCOME_UNKNOWN` whether the rejection appears to be a timeout, transport failure, or node-side refusal. A synchronous throw before the request promise is returned is a definite local pre-submission failure. The exact signed block is durably recorded first and must be reused for reconciliation.

`prepareBlock()` is not given a superficial whole-operation deadline. It may perform internal RPC calls and PoW, so the global SDK owner remains held until the composite call settles.

### Durable PoC journal

The ignored `.runtime/settlement-journal.json` file stores a versioned, checksummed set of exact payment attempts. Same-directory temporary writes, file sync, atomic rename and directory sync protect each revision where the operating system supports them. An initialization marker detects a missing state file after the first successful write. Invalid state fails closed; deletion of both state and marker is outside this local-filesystem trust model. Schema v1 remains readable and is not upgraded to v2 by reads, ordinary updates, or disabled retention. The first successful tombstone conversion atomically writes schema v2 in one revision and extends the checksum over active records and tombstones. Rollback to a v1-only reader is unsupported after v2 appears.

The journal has a default active-record capacity of 256 and a fixed separate tombstone capacity of 4096. Both maps remain inside `maxFileBytes`; tombstones participate permanently in replay and uniqueness checks and are not silently evicted or archived. Capacity failure preserves the full record. The journal is single-process, single-writer and single-host. Cached resource responses are plaintext JSON limited to 64 KiB. It does not implement distributed locking or production exactly-once delivery.

### Evidence and delivery

Internal states describe evidence rather than assumed finality:

```text
VALIDATED (offline checks complete; journaled before publication)
    |
    +--> SUBMISSION_OUTCOME_UNKNOWN
    |
    v
SUBMISSION_ACKNOWLEDGED
    |
    v
MOMENTUM_INCLUDED
    |
    v
DELIVERY_PENDING (claim recorded; execution may have begun)
    |
    v
DELIVERED
```

An account lookup showing `confirmationDetail` establishes only `MOMENTUM_INCLUDED` under the current node-observation policy. It does not prove irreversible finality or merchant receipt. Observed inclusion evidence is never downgraded by a later transient lookup failure.

The HTTP boundary returns a distinct `409` recovery response for uncertain or pending outcomes. It directs the buyer to reuse and reconcile the same payment. It does not release the protected resource.

`reconciliationRetentionMs` is a constructor-only opt-in, defaulting to `null`, with an inclusive enabled range of 3,600,000 through 2,592,000,000 milliseconds. Only explicit zero-argument `runReconciliationMaintenance()` calls evaluate persisted `createdAt`; ordinary requests, startup, environment configuration, and background work do not. Each serial call examines at most 64 deterministic candidates. Backward clock movement delays terminalization, while a forward jump can terminalize early.

Maintenance queries only the exact transaction hash. Included evidence advances the full record to `MOMENTUM_INCLUDED`, exact unconfirmed evidence advances it to `SUBMISSION_ACKNOWLEDGED` when stronger, and unavailable lookup retains it. Only exact absence after age can atomically replace an eligible `NONE`-delivery record with a tombstone. Existing tombstones are rechecked during explicit maintenance even when new terminalization is disabled; first late inclusion is recorded without invoking delivery or authorizing retry, resource access, refund, credit, or replacement.

Candidate enumeration releases the journal writer before lock acquisition. Each candidate takes the payer queue before the process-global live-SDK owner and holds both through a fresh journal snapshot, exact-hash observation, and final compare-and-replace. The CAS rechecks the global revision and complete record identity/evidence/delivery state. This prevents same-process publication from crossing the final boundary but provides no distributed exclusion or restart-durable maintenance cursor.

An exact tombstone match returns a local response-only HTTP `402`: `PAYMENT-RESPONSE` contains bound network, transaction, payer, retained prior state, and fixed `payment_reconciliation_terminal`; `PAYMENT-REQUIRED` and retry are absent. The buyer accepts only that exact disjoint shape and creates no recovery owner. Malformed or mismatched terminal candidates fail closed, while the existing dual-header `payment_settlement_failed` `402` and recoverable `409` behavior remain unchanged. This is retention abandonment rather than inclusion, irreversible finality, supersession, or safe replacement authority. Installed official-parser behavior is characterization only, not standardization, official Zenon support, release, or activation.

Concurrent identical HTTP requests converge within one process. A `DELIVERED` cached response is returned without republishing or repeating the callback. Arbitrary external side effects still have a crash window between callback execution and the durable delivered record.

Live resources require HTTPS. The paid request uses manual redirect handling and treats redirects or unverifiable post-submission responses as uncertain. `/paid` responses are non-cacheable and vary on the payment header. Delivery requires an affirmative durable claim before the resource callback executes.

### Optional observed-confirmation gate — implemented locally, pending review

Live requirement construction optionally reads `ZENON_MINIMUM_MOMENTUM_CONFIRMATIONS`. Missing, empty, and canonical string `1` keep the signed field absent and the effective threshold at one. Canonical strings `2` through `30` emit numeric `extra.minimumMomentumConfirmations`; all other spellings, representations, accessors, and ranges fail before asset resolution or live effects. Mock mode never reads the variable and rejects the field. Wire absence means one, while a present field must be a numeric integer from 2 through 30; numeric `1` is noncanonical.

The implemented Option-A flow preserves `maxTimeoutSeconds` as the first-inclusion wait budget. It journals the first exact inclusion immediately. If the node-reported `confirmationDetail.numConfirmations` is below the signed `N`, delivery remains `NONE` and the resource server emits an authenticated same-payment `409` with `PAYMENT-RESPONSE`, no `PAYMENT-REQUIRED`, `payment_reconciliation_required`, and `reuse_and_reconcile_same_payment`. A later identical payment request performs at most one fresh exact-hash lookup after readiness. Only a higher count for the identical inclusion hash, height, and timestamp tuple strengthens evidence. Equal, lower, drifting, absent, and unavailable observations preserve the strongest durable record and never release the resource. A durable count already at `N` needs no node read to authorize the ordinary delivery path.

`SUBMISSION_ACKNOWLEDGED` and `SUBMISSION_OUTCOME_UNKNOWN` never republish; only the existing `VALIDATED` exact-block path remains idempotently publishable. Delivery claims pass the full accepted requirement into the journal and reauthenticate it against the persisted payment-intent preimage under the writer lock before every early return. No journal schema, record, checksum, or tombstone format changed; the existing schema-v2 retention behavior and default-one compatibility remain. Configuration rotation is fail-closed availability: an unresolved payment created under another `N` remains unavailable until its original configuration is restored. Rolling back to an older reader while unresolved threshold-bearing records exist is unsupported. Generic clients may not retry this PoC-specific `409`, so non-default `N` requires PoC same-payment retry support.

The threshold is only an operator policy based on one operator-trusted node observation. It is not protocol finality, canonicality or reorganization safety, authenticated chain identity, independent attestation, recipient receive, or spendability. Evidence version 1 remains default-one-only: all evidence-v1 runners reject any explicit `minimumMomentumConfirmations` before filesystem, runtime, SDK, RPC, wallet, signing, payment, or publication effects. A future non-default-`N` capture requires a new evidence version and separate design.

The implementation and focused offline tests are complete locally but remain pending independent review and integration. This tranche executed no new live payment and makes no new evidence, release, activation, finality, or production-readiness claim. Issue #81 different-operator verification remains separate, deferred, and nonblocking. Phase 2C remains deferred.

### Unconfirmed pagination

The facilitator reads every page implied by the RPC `Count`, rechecks the first page for a changing snapshot, and fails closed on malformed or incomplete results. The PoC accepts at most 200 unconfirmed blocks for one payer; larger results fail closed rather than enter an unbounded loop.

## Selected near-term path: operator-trusted historical observation

Issue #45 selected Path B as the bounded route to empirical testnet feedback. The CLI registry contains one immutable historical observation derived from the height-2 example in `zenon-network/znn-wiki` at the pinned 2021-12-17 source revision. The selector has no default or floating alias, accepts no environment-supplied chain-profile fields or trust-artifact URL, and requires the existing testnet-only acknowledgement plus a distinct operator-trust acknowledgement.

Inside the exclusively owned SDK session, the current policy requires exact agreement for:

1. the historical observation's Momentum version, height and identity;
2. its exact `chainIdentifier`;
3. its exact predecessor, used as the profile's `genesisMomentumHash` field;
4. the ordinary synchronized-frontier chain-identifier check.

This is a declared operator assumption, not authenticated chain identity. The source is unsigned, the predecessor observation does not independently prove current genesis semantics, and the RPC response does not prove continuity to the frontier. Honest mismatches or resets that alter the pinned tuple fail closed; forks after height 2 and disconnected or malicious RPC views are not detected. Path A remains the long-term research path: an authoritative trust artifact plus verified checkpoint/header linkage, SPV, or a light-client design belongs in the separate authenticated-policy path.

The current CLI supports one profile generation in the shared journal namespace. Profile rotation and rollback are unsupported. Before another profile can be selectable, journal state must be isolated and cross-profile maintenance, recovery, and rollback behavior must be tested.

The offline wiring does not complete Issue #45. A separate operational gate must use a disposable, minimally funded testnet wallet, perform the first real settlement and delivery, and publish the agreed verification evidence. No such live run is claimed here.

## Intermediate local four-node devnet lane

The first devnet slice defines the canonical local profile artifact and its offline observation checks. Ordinary buyer and server CLIs can select that family only through the closed, default-off selector with the exact local lane, a genuine canonical artifact, the exact local acknowledgement, and a canonical loopback WebSocket RPC input. Selection has no alias or fallback and permits ordinary runtime construction, requirement construction, server setup, and direct readiness wiring, but the owned live SDK session rejects the local policy before SDK effects. `src/zenon/operator-trusted-local-devnet-profile.js` parses one exact canonical `operator-trusted-self-created-local-four-node-devnet-v1` artifact and validates a supplied chain observation without RPC access. The artifact fixes its own acknowledgement, chain identifier 69, generated genesis predecessor and height-two Momentum, external-generator repository and immutable revision, node source repository and immutable revision, and container image by `sha256` digest. It has no environment override, floating image tag, endpoint, path, wallet, credential, node key, payment, transaction, or signature field.

The dedicated check requires the frontier and height-two observations to agree with that artifact and returns branded non-authenticating evidence. This establishes only internal consistency for operator-supplied declarations. It does not authenticate the generator, source revision, container contents, RPC endpoint, chain identity, genesis, or frontier lineage. The fixed semantic claim is reproducible equivalent behavior rather than byte-for-byte recreation of a private network whose timestamps and secret random material change per generation.

"Four-node" names the intended external operator workflow only; the artifact does not verify node count, roles, topology, or a topology digest. The exact false nonclaim records that limitation; a topology schema is deliberately deferred beyond this slice.

The merged, offline-tested policy-bridge slice adds a closed two-family dispatcher and an artifact-bound local policy. The exported direct local observer and dispatcher consume genuine local policies and evidence, while direct `assertZenonNodeReady` is the only payment-readiness integration that accepts the local family. Direct readiness revalidates the evidence family and requires the exact current observer result after an injected read before accessing evidence fields. Module import and policy construction are side-effect-free and offline; direct policy observation and direct readiness invocation perform the injected node reads. The local adapter snapshots descriptor-safe SDK Momentum and fixed-length Hash data, captures the frontier before the single height-two query, rejects mutation after the await, and returns only same-family branded non-authenticating evidence. Policies and evidence, together with their copied fields, are detached and deeply frozen; validation uses detached normalized copied snapshots. The SDK frontier object is returned unchanged by readiness and is not claimed to be detached or frozen. Adapter-controlled Promise handling uses captured intrinsics, but it cannot undo thenable assimilation or other behavior already performed inside an injected SDK method before that method returns a genuine native Promise. Local evidence does not populate `authenticatedProfile`.

The closed ordinary CLI selector supports two exact families without aliases or fallback. Public-testnet selection retains its existing profile and acknowledgements. Local selection requires the exact local lane, genuine canonical artifact, exact local acknowledgement, and validated canonical loopback RPC input. The client and facilitator constructors accept only the matching local-policy/RPC pair, while owned live sessions, the role probe, and the Gate-B runner remain public-testnet-only. Ordinary valid public-testnet selection and payment semantics remain unchanged; SDK network-ID behavior is unchanged. The bridge retains the non-enumerable readiness-result assimilation shield and earlier hostile-input rejection. At the owned live-session boundary this remains offline-tested, runtime-unregistered readiness plumbing, so the local lane provides no payment/evidence execution, authenticated chain identity, release, activation, production readiness, or Issue #45 completion.

The merged, offline-tested readiness-runner slice adds only a dedicated explicit opt-in local-devnet readiness runner, its fixed-output CLI, one internal Worker, focused tests, and these documentation updates. The public API accepts one descriptor-validated opaque options object and resolves only primitive `true`. Parent import and preflight perform no network I/O. A run requires the genuine artifact and exact acknowledgement, a restrictive current-directory artifact file opened and read through one checked descriptor generation, a bounded timeout, and exactly `ws://127.0.0.1:<non-default-port>/` or `ws://[::1]:<non-default-port>/`. The canonical decimal port must be explicit and cannot be 80, and the trailing slash is required. Hostnames and DNS, `wss`, credentials, implicit or default ports, alternate encodings, extra paths, queries, fragments, percent encoding, whitespace, and other noncanonical forms are rejected. Direct invocation uses only `node src/local-devnet-readiness-runner-cli.js` with exactly one each of `--artifact-file`, `--rpc-url`, `--acknowledgement`, and `--timeout-ms`. That readiness-runner slice did not add a package script, environment alias, selector, ordinary CLI, or public runner; the later closed selector permits explicit local policy selection for ordinary runtime construction but does not register or invoke this runner.

Each invocation uses one single-use Worker and a new non-singleton SDK client. Reconnect and redirect behavior are disabled, Worker output is captured and discarded under a bound, and the read adapter exposes only network information, sync information, frontier Momentum, and the exact height-two query needed by `assertZenonNodeReady`. A successful run performs one connection and exactly four ordered readiness reads. It mutates neither SDK NetworkID nor ChainID, closes the SDK client once, and returns success only after Worker exit and both output-pipe close barriers are proven. Timeout, protocol ambiguity, hostile SDK shapes, unsafe close, or unproven destruction fails closed; unproven destruction poisons later use in that parent. Adapter-controlled native-Promise settlement cannot undo behavior already performed inside the pinned SDK producer before the genuine Promise is returned.

This slice operationalizes readiness only. It has no wallet, funding, signing, block construction, publication, facilitator, buyer/server HTTP, protected-resource delivery, journal, live-evidence capture or bundle, proof, or payment capability. The artifact, builder and runtime provenance, chain observations, and intended four-node label remain operator-trusted and non-authenticating; `fourNodeTopologyVerified` remains false. It does not authenticate identity or topology, activate a runtime lane, prove a public-testnet Gate B run, release a feature, or establish production readiness. Even a later successful local readiness run remains an intermediate milestone and cannot complete Issue #45.

`0x3639/testnet` stays an external operator tool: no code is copied, vendored, patched, redistributed, or added as a dependency. A later payment/evidence runner design must separately resolve the descriptive network label, SDK network-ID binding, local acknowledgement, loopback-only transport, the external tool's seed-plus-four-pillar/five-service workflow description, immutable image provenance, protected operator material, isolated journal/evidence state, and devnet evidence classification. `fourNodeTopologyVerified` remains false unless topology is independently authenticated. Wallets, credentials, node keys, endpoints, paths, transactions, signatures, Docker outputs, and generated packages stay outside this repository/artifact boundary. Even a successful local run is an intermediate milestone; it cannot complete Issue #45 and does not replace the later public-testnet evidence gate.

## Issue #45 staged live-evidence work

| Milestone | Repository status | Validation status | Operational boundary |
| --- | --- | --- | --- |
| Historical public-testnet profile | `MERGED` | `OFFLINE_TESTED` | `NOT_AUTHENTICATING` |
| Offline evidence contract | `MERGED` | `OFFLINE_TESTED` | `NOT_ACTIVATED` |
| Operational evidence runner | `MERGED` | `OFFLINE_TESTED` | `NOT_ACTIVATED` |
| Local artifact and explicit policy selection | `MERGED` | `OFFLINE_TESTED` | `EXPLICIT_DEFAULT_OFF` |
| Local readiness runner | `MERGED` | `OFFLINE_TESTED` | `RUNTIME_UNREGISTERED` |
| Public-testnet Gate B | `WS_ONCE_OFFLINE_TESTED` | `LIVE_CAPTURE_RETAINED` | `PENDING_INDEPENDENT_REVIEW_WORKSPACE_QUARANTINED` |

Except for the explicitly recorded Gate-B observation below, these status tokens describe repository integration and offline validation. Merged work is not thereby released or enabled by default. The historical public-testnet profile and operational runner remain behind their exact explicit opt-in gates. The local policy is explicitly selectable through the closed ordinary CLI selector, but the separate readiness runner remains runtime-unregistered and owned live sessions remain public-testnet-only. Operator-trusted evidence does not authenticate chain identity, local topology remains unverified, and local readiness cannot replace public-testnet Gate B. The observed run did not satisfy the independent-verification or evidence-version-1 bundle-publication gates, and clean end-to-end controller closure was not proven. A separate operator-trusted same-route record can meet Issue #45's narrower Path-B acceptance boundary only after human disclosure review and merge.

PR A is the merged, offline-tested pure evidence contract only and is part of Issue #45 without completing it. It is not activated and has not been live-executed. Its six-path boundary adds `src/live-evidence.js`, `src/live-evidence-cli.js`, `test/live-evidence.test.js`, and narrow updates to `README.md`, `SECURITY.md`, and this plan. It changes no settlement, buyer, server, journal, wire, live-runtime, dependency, workflow, or ordinary CLI behavior.

Evidence version 1 uses five exact typed fragments: a manifest, an operator-supplied declared complete public account block and declared confirmation observation, a decoded HTTP exchange, an asserted final journal record, and externally captured timing assertions. Offline validation proves only structural and cryptographic consistency among those declared observations. It does not independently authenticate inclusion or finality, facilitator publication, HTTP delivery, or buyer receipt. Assembly requires one advertised offer at selected index zero, reuses the existing network-free Zenon preflight and binding primitives, cross-binds the signed block, resource, intent, authorization, confirmation, response and delivered journal state, and derives the descriptive Plasma/PoW class from signed fields. The verifier performs the same complete validation on a detached snapshot and never queries a node.

The parser detects duplicate decoded keys before object construction and applies exact schemas, descriptor-safe snapshots, bounded depth/count/byte limits, deterministic canonical JSON, eight section digests and one domain-separated final digest. Timing events are exact and once-only, sequences preserve the cross-role protocol edges, role-scoped monotonic clocks recompute durations, and UTC values supply only explicit correlation and ordering assertions. The template is a null checklist rather than a fragment or bundle. The file CLI exposes only `template`, `assemble`, and `verify`, accepts no environment, standard-input, network, wallet, journal or server capability, and emits fixed sanitized status lines.

PR B is the merged, offline-tested operational-preparation slice and remains part of Issue #45 without completing it. Merging PR B neither activated it nor performed a Gate-B run; the later explicitly authorized observation below was a separate operator action. Its ten-path boundary adds the import-safe observation, coordinator runner, fixed-output runner CLI, facilitator worker, and focused runner tests; it modifies only the optional lifecycle seams in `src/zenon-payment.js`, their live-settlement tests, and these three documentation files. It leaves the ordinary buyer/server CLIs, buyer and resource-server behavior, live runtime, settlement journal implementation/schema, PR-A contract/CLI/tests, configuration files, packages, dependencies, and workflows unchanged.

The effectful runtime has three logical roles in two processes. The coordinator owns the runner and buyer and privately retains the buyer module's process-local WeakMap recovery owner. A separately spawned facilitator child owns the facilitator SDK singleton, loopback resource server, fresh journal, and fixed protected-response delivery adapter. The exceptional CLI adds a non-effectful fixed-output supervisor parent. IPC carries exact, bounded, allowlisted, request-correlated and cause-free protocol data; secrets and recovery owners never cross it. A facilitator restart is allowed only after a poisoned child has acknowledged stop and its operating-system exit has been confirmed; coordinator loss after submission is armed permanently marks the evidence incomplete. PR B adds no persisted recovery capsule and no resume-without-signing interface.

The strict public config binds the exact operator-trusted profile, both acknowledgements, one expected challenge/offer, recovery limits, and the public HTTPS `/paid` resource. Three separate owner-only files hold buyer RPC, buyer wallet and facilitator RPC inputs under one protected workspace. Component-wise path checks, safe-open generation checks, bounded reads, private modes, link/alias rejection and atomic output rules apply. Public DNS answers must be globally routable, resolve under a deadline, and remain pinned to the subsequent HTTPS health/challenge/payment/reconciliation requests. DNS, health, unsigned challenge, and unrelated requests retain the validated base timeout. Exact signed payment and same-payment reconciliation use the accepted inclusion window plus twice that validated RPC timeout only after exact challenge equality and bounded decoded-payment preflight against the immutable expected resource, requirement, intent and transaction identity. The first successful cryptographic preflight privately binds the exact encoded header bytes; reconciliation is synchronous byte-equality selection and cannot validate or send a replacement. Overflow or a result above the 420-second schema-derived maximum rejects without clamping or fallback. Current Gate-B values produce a 120-second response deadline. The operator remains responsible for external TLS ingress; PR B neither provisions nor contacts it in tests.

The second current-testnet milestone adds a fresh closed WSS one-shot family for exactly `wss://rpc.testnet.zenon.info/`. It reuses the existing operator-trusted Chain-73404 profile and the controller, private workspace, quick-tunnel HTTPS ingress, source-attestation, one-shot execution, capture, and independent-publication boundaries. Coordinator/bootstrap, review, and Phase-3 inputs use schema version 2; the protected endpoint source uses version 2; RPC role inputs use secret version 3; the run uses `current-testnet-wss-once-v1` and runner version 3; authorization uses version 3; the independent reviewer returns result version 2; and the configuration digest uses domain `zenon-x402-current-testnet-wss-once-config-v1`. Every value is validated and cross-bound as one generation. Other endpoints and every old, mixed, partial, replayed, or cross-family tuple are rejected without conversion or fallback.

The plaintext current-testnet one-shot family and historical generic-WSS runner remain unchanged and disjoint. The new WSS family omits only the plaintext-transport exception acknowledgement. It retains the testnet-only, operator-trust, WSS one-payment, publication-hold, quick-tunnel telemetry, and final exactly-one-payment/no-recovery acknowledgements. Both current-testnet one-shot families use the same durable consumed-marker name, so a workspace is one-use across the families. Preparation and execution require a fresh workspace; old, mixed, consumed, or quarantined artifacts have no migration, retry, resume, or in-place upgrade path.

TLS encrypts the RPC connection and authenticates its server name under the host trust store, but it does not authenticate canonical chain identity, operator statements, frontier lineage, finality, recipient receipt or spendability, or node-binary provenance. Public genesis, configuration, and node-plan material are mutable operator-published snapshots, not signed immutable anchors. The WSS run retains publication-ineligible private candidate-v2 metadata with WSS confidentiality and TLS server-name authentication true, authenticated chain identity false, operator trust and independent verification required, and the endpoint undisclosed. The quick tunnel continues to provide only the protected HTTPS resource ingress. Its readiness hardening permits bounded expected startup absence or exact not-ready responses while preserving one tunnel identity, then requires three stable ready observations. Later checks tolerate only bounded same-identity absence or canonical 503/zero readiness and emit `CHECKED` only after fresh ready state; malformed, drifting, regressing, late, or timed-out states fail closed. Source implementation and offline tests alone are not live-payment or delivery evidence. The observed run below remains short of independent verification and evidence-version-1 bundle publication; clean controller closure was not proven. Issue #45 remains open until the separate operator-trusted record receives human disclosure review and is merged.

### Observed Gate-B run boundary (2026-09-04)

One execution using a freshly generated, dedicated, disposable testnet wallet recorded one x402 v2 `exact` upfront payment of one atomic unit of ZNN under the descriptive `zenon:testnet` label and operator-trusted Chain 73404 profile. The signed block used the PoW branch with `fusedPlasma` 0 and difficulty 34,764,000. The measured 402-to-200 interval was 24,375 ms: `prepareBlock()` was 6,887 ms; buyer SDK ownership was held for 8,566 ms after a 0 ms wait; the publish-RPC acknowledgement interval was 238 ms; inclusion wait was 11,973 ms; delivery was 29 ms; and facilitator SDK ownership was held for 15,611 ms after a 0 ms wait.

The retained journal is schema 1 at revision 5 with one active `MOMENTUM_INCLUDED` / `DELIVERED` record. The exact six-field protected response body remains in the private capture and is projected only into the separate operator-trusted record rather than reproduced here. The five retained fragments came from the uninterrupted, non-recovery inner runner path and were assembled and verified in memory. A separately implemented post-run read-only comparison against the same operator-trusted route observed one account-height advance, the exact one-atomic-unit debit, exact signed-block identity and payment-intent/resource bindings, and a later Momentum. This was not protocol reconciliation and granted no retry, resubmission, or replacement-payment authority. The same route remains operator-trusted and does not satisfy the independent operator-route gate for evidence-version-1 publication.

The non-recovery inner runner reached private pending capture before the outer operator front end reported post-delivery closure-proof quarantine. The later absence observation does not prove which clean-closure predicate failed and does not upgrade the terminal result to clean shutdown. It also does not invalidate the retained payment or delivery observations. The workspace remains consumed and quarantined with no reuse, resume, retry, or in-place upgrade.

No protocol finality, authenticated chain identity, recipient receive or spendability, facilitator authorship, independent HTTP attestation, or clean shutdown is claimed. No independent operator-route verification or public evidence-version-1 bundle exists yet. The unchanged evidence-version-1 workflow requires a human-approved different-operator-route observation and an accepted strict independent-review assertion record before the retained fragments are supplied to the network-free assembler and verifier. Evidence-version-1 bundle publication still requires that observation, the accepted assertion record, successful retained-fragment assembly and offline verification, separate publication authorization, and an explicit cleanup-quarantine addendum kept outside the version-1 JSON bundle.

The separate `docs/evidence/gate-b-operator-trusted-observation-2026-09-04.json` record is explicitly not evidence version 1. It contains five allowlisted projections made directly from the retained manifest, chain, HTTP, journal, and timing fragments. Its `operator-trusted`, `same-route`, and `not independently verified` labels are publication classification, not fragment-derived measurements. The JSON omits the separate post-run comparison, outer-controller closure and quarantine, transport-authentication, and explorer-route assertions because the five retained fragments do not mechanically supply them. Tests recompute the payment-intent/block-data binding, transaction and chain relationships across the projections, the decoded HTTP and journal relationship, and every duration from retained timing events. The initial HTTP projection contains status and observation time only and therefore does not prove the exact 402 body or header bytes. The original run-time publication hold remains binding for the five private fragments and evidence-version-1 bundle. A later explicit, narrowly scoped post-run disclosure decision and human review can authorize this distinct record without retroactively changing run authority. After human disclosure review and merge, the record satisfies Issue #45's bounded operator-trusted Path-B acceptance criteria without satisfying or weakening the independent evidence-version-1 gate. Issue #45 remains open until then. Clean controller closure is not an Issue #45 acceptance criterion. A fresh successful run is required only if the project separately wants to demonstrate clean end-to-end controller closure.

The Issue #83 slice adds an inert, network-free self-consistency library and an explicit-path fixed-output file CLI for that separate operator-trusted record. The library accepts only bounded round-trip UTF-8 text or bytes and returns primitive `true` or throws one fixed cause-free error. Its duplicate-aware parser enforces canonical nonnegative safe-integer tokens, paired surrogates, exact order-insensitive object allowlists, and depth/node/member/string bounds for both the outer record and nested HTTP body. Relationship checks reuse only `canonicalJson`, `paymentIntentDigest`, `sha256Hex`, and the pure x402 wire validators. They cover the exact operator-trusted same-route/evidence-v1-false classification, false record nonclaims, one offer at selected index zero, intent/Base64/resource bindings, retained partial-block and journal equality, amount/asset/payee/payer/network/full-chain-profile links, the supplied transaction and confirmation tuples, confirmation height ordering, shared response/cache status/content type/six-field body, final journal state and counts, the exact 21-event graph, role-local clocks, declared UTC order, twelve durations, and HTTP/confirmation/journal anchors.

This is intentionally a self-consistency check, not validity, proof, or authenticity; a coordinated forged record can pass. It does not establish committed-byte provenance, secret absence, a full block hash, signature or payer-key binding, PoW, Plasma, transaction existence, authenticated or canonical chain identity, frontier lineage, finality, recipient receipt or spendability, facilitator authorship or publication, complete HTTP-response equality, buyer receipt, clock truth, cross-domain attestation, clean shutdown, evidence version 1, release, activation, or production readiness. It does not call the evidence-v1 verifier or Zenon transaction preflight, reconstruct omitted fields, read a node, reopen a wallet, sign, publish, alter a journal, or change runtime behavior. The public transaction linkage remains deliberate record data and is never printed by the CLI. Object whitespace and key order are not provenance, arrays retain their order, and Unicode is not normalized.

The CLI requires exactly `--file <explicit-path>` and has no positional/default/search/environment/telemetry/output/runtime path. It requires no-follow support and opens one regular single-link file of at most 64 KiB using no-follow plus nonblocking and close-on-exec flags when available. Every component and the leaf are checked before and after the single bounded read, and the leaf is also checked through its open descriptor; device, inode, mode, link count, size, modification time, and change time must remain identical. Success and failure expose only their fixed status line and exit code. The implementation changes exactly `src/operator-trusted-observation.js`, `src/operator-trusted-observation-cli.js`, `test/operator-trusted-observation.test.js`, `package.json`, `README.md`, `SECURITY.md`, and this plan. It changes no dependency, workflow, committed observation JSON, evidence-v1 module/test, payment/runtime/journal behavior, or publication state. The already recorded run is not rerun or upgraded. Issue #81 remains deferred; this slice neither blocks nor closes it.

The older plaintext transport exception is a separate closed family. `preflight-public-ws-once` and `run-public-ws-once` require the exact switch, protected authorization and hostname-source files, schema-v2 RPC role inputs, the distinct current-testnet operator-trusted height-2 profile, one run name, source revision, complete configuration digest, payment-intent digest, value-equal buyer/facilitator endpoint, the complete canonical quick-tunnel block, and exact payment/publication acknowledgements. The configuration digest is `SHA-256(UTF-8("zenon-x402-public-ws-once-config-v2\n" + canonicalJson(parsedConfig)))`, encoded as exactly 64 lowercase hexadecimal characters, so authorization binds the complete parsed configuration rather than selected duplicate fields. Run mode performs three raw-tree attestations with shell-free bounded calls to a fixed Git executable from the canonical module-derived repository root. Each requires checked-out `main`, equality among `HEAD`, local `main`, local `origin/main`, and the bound revision, and an exact frozen-commit tree manifest from non-helper `ls-tree` plumbing. Pure JavaScript then checks every tracked regular file's stable raw bytes, Git blob ID, identity, and exact mode without consulting the index or invoking clean/smudge filters; it rejects symlink, submodule, unsafe-path, mode, tracked-byte, and every untracked `src` entry, including ignored entries. All Git output and diagnostics remain suppressed.

Attestation one precedes parent worker-module preload; attestation two follows that load and is immediately before consumption. After marker creation, the public-only controller forks an idle child and performs an exact enum-only `PRELOAD` / `PRELOADED` exchange. Receipt proves the child's static ESM graph initialized without calling the runtime start path, so no RPC, wallet access, journal creation/load, resource listener, signing, or publication is initiated. Attestation three then runs before buyer readiness and `START`, and the lane initiates no repo-local path load afterward. Preload or third-attestation failure reaps the child and preserves the already-consumed marker. Ordinary WSS sends no preload message and retains its existing `START` protocol.

The attestation covers committed tracked repo-local bytes against trusted local repository metadata. It does not attest ignored or untracked content outside `src` or installed dependency bytes under `node_modules`; only the tracked dependency declarations and lockfile are covered. Local `origin/main` is a cached ref, not network freshness, and no fetch occurs. Precisely timed mutation and restoration by an already-authorized same-UID actor remains outside the claim. The profile pins the height-2 hash and previous hash reproduced by two fresh reads from the same source; this is not independent chain authentication. The endpoint parser accepts only canonical credential-free numeric public-IP `ws://` with an explicit non-default port and root path. The ordinary WSS parser, historical profile, CLI, worker mode and defaults remain unchanged and cannot alias or fall back into this lane. SDK testnet NetworkID remains fixed independently of the current chain identifier.

The exceptional CLI parent imports neither the runner nor the SDK. It supervises one fixed child with empty arguments and environment, ignored standard streams, one dedicated bounded option pipe carrying exactly six local paths, the run name, and the transport-exception token, and an exact request-correlated enum-only process-control protocol. The parent enforces structural limits and the child performs semantic validation; neither channel carries RPC endpoint or wallet contents, and control IPC carries no paths. Standard-stream output is discarded. A result is accepted only after the matching terminal enum and clean close; handled protocol/process faults escalate termination and are reported only after child close. A supervisor IPC disconnect handled by the child's event loop triggers fail-closed exit, but this is not OS-enforced parent-death protection. This is output containment, not a same-UID sandbox.

Exceptional preflight is filesystem-only and does not read wallet contents. Execution exclusively creates and durably syncs one workspace-root consumed marker before any worker, RPC, wallet, signing, or publication effect. Open workspace and run-directory handles pin device/inode identity, and handle/path identity, type, ownership, and mode are compared before and after every effect and pathname state boundary. Drift fails closed. The marker and run directory survive every failure; existing or partial marker state consumes one attempt while the owner-controlled workspace namespace remains unchanged. Node has no portable `openat`-style creation API, so an active same-UID actor can race between checks and already controls the protected input files. No global one-shot or hostile same-UID confinement is claimed. Recovery attempts and delay are zero. Any uncertain/acknowledged publication, timeout, disconnect, process restart, repeated phase, or crash hard-stops without same-payment recovery, replacement payment, another challenge, wallet reopen, or new signature.

The uninterrupted exceptional flow's run-created retained tree contains exactly the consumed marker, `SUBMISSION_ARMED`, the initialized schema-v1 settlement journal, and private pending metadata plus five typed capture fragments; the six protected inputs remain unchanged. Metadata declares plaintext transport, no confidentiality or peer authentication, explicit operator risk acceptance, RPC-endpoint nondisclosure, and `publicationEligible: false`. The capture contains no plaintext RPC endpoint, wallet secret, local path, evidence directory, `candidate-bundle.json`, or `COMPLETE`; the deliberately public HTTPS resource URL remains part of the payment evidence. A later manual gate must use a different independently operated WSS/HTTPS node or explorer to confirm the exact block, payment-intent binding, and Momentum inclusion. Once that independent record is supplied, the retained fragments are sufficient for the existing assembler and verifier; the same endpoint or operator route is insufficient. The anchor remains independently unverified; public-genesis derivation, authenticated chain identity, canonical lineage, facilitator authorship, finality, recipient receipt, reproducible binary, release, activation, and production readiness remain nonclaims.

Run mode performs buyer and facilitator wallet-free readiness before listening or signing, then requires the exact health and 402 challenge before the coordinator reads the disposable wallet. Observation is branded, synchronous, payload-free, free-form-metadata-free and default-off. It captures the exact 21 PR-A phases at their true lifecycle boundaries, with coordinator-assigned global sequence, role-local monotonic clocks and absolute UTC assertions. Inclusion UTC is captured exactly once when valid inclusion is first observed and is reused by the inclusion event and journal evidence without allowing an observer clock to choose durable data. Clock callbacks are never awaited and receive no receiver, payload, or free-form metadata; thrown or invalid results are contained without changing settlement, although arbitrary synchronous clock code can consume local execution time. Observation failure permanently makes the run nonpublishable.

Evidence version 1 accepts only an uninterrupted first-attempt success with every phase once. For the historical WSS runner, same-payment recovery retains only each in-memory successor owner, never repeats challenge acquisition, wallet access, payment construction or signing, and has both attempt and absolute elapsed bounds. Both current-testnet one-shot families invoke no recovery or restart path and only quarantine ambiguous state for manual independent resolution. Every recovery, child restart, repeated phase, cached delivery, ambiguous publication acknowledgement, observer loss or crash is resolution-only and permanently ineligible for a version-1 bundle. If the coordinator dies after submission is armed, the journal remains protected for manual safe resolution and no replacement payment may be made.

The clean run starts from a schema-v1 revision-zero empty journal with maintenance disabled. After resource-server closure and quiescence, one final `SettlementJournal.load()` returns the coherent one-record snapshot; the runner never calls `list()`. The uninterrupted transition sequence fixes revision five: create, publication acknowledgement, inclusion, delivery pending and delivered. Schema v2, reused/multiple records, nonquiescence, unexpected generation, cached-response mismatch, or an unexpected retained-tree entry fails closed. PR-A's five fragments are parsed, assembled, and verified in memory; the exceptional lane atomically retains only those fragments under the private pending state. It neither persists nor publishes a candidate bundle.

Gate B is a separate authorization boundary for the actual public-testnet endpoint, a disposable, minimally funded, single-use wallet, one payment, live capture and deliberate publication of linkable evidence after human review. Its frozen publication package includes the complete exact six-field default protected response body rather than only a digest, plus absolute UTC correlation timestamps rather than relative-only timing. Gate B does not require Phase 2C or hardware-wallet support. PR B implementation and test validation perform no live run, and run mode remains default-off. Merging PR B does not release or activate the operational runner. Neither PR A nor PR B completes Issue #45, claims authenticated chain identity, enters Phase 2C, adds hardware-wallet support, or establishes production readiness.

### Gate B operator handoff

1. Obtain separate authority for the external operational inputs and a private workspace; commit none of their values.
2. Outside the repository, have the identified operator create and review a disposable, single-use test wallet, then fund it minimally under a separate authority. The optional fixed-output local helper can create only the protected wallet and address files; it does not fund or authorize them.
3. Use the exact current-testnet WSS family for the intended live attempt and require its preflight to succeed before authorizing run mode.
4. Treat only an uninterrupted evidence-eligible result as a publication candidate; recovery, restart or ambiguity remains nonpublishable.
5. Apply the existing offline verifier, then human-review every intended public string and the deliberate disclosure boundary.
6. Obtain separate publication authorization. Issue #45 remains open until the live execution and published evidence have been independently reviewed.

### One-shot native funding receipt

The source-only macOS faucet-receive helper is a separate authority boundary between wallet creation and Gate-B preflight. It is not selected by an existing runner, controller, package script, environment alias or default. Empty arguments preserve the legacy workspace lane; the only generated-workspace CLI form is exactly `--workspace <absolute-generated-workspace>`, and an explicit legacy path is rejected. There is no environment, default, alias or normalization fallback. The generated path must be a direct child of the current user's canonical Application Support root and carry the wallet helper's exact 32-lowercase-hex generation suffix. The fixed-output CLI consumes one bounded private descriptor-3 frame containing only schema version 1, one canonical numeric public `ws://` endpoint, and the exact acknowledgement `I_CONFIRM_THE_TWO_PENDING_NATIVE_TESTNET_SENDS_ARE_THE_INTENDED_FAUCET_FUNDING_AND_AUTHORIZE_ONE_POW_RECEIVE_FOR_EACH`. This acknowledgement is operator confirmation of the observed source class, not authenticated faucet identity, sender provenance, canonical chain identity or permission for any x402 payment. The supervisor passes generated selection only as the child's current directory; bootstrap and control IPC remain path-free, and the child independently reparses that current directory against the canonical Application Support root. Control IPC is primitive-only: one exact fresh, partial-recovery or read-only-recovery mode, indexed `PUBLISHING_0`/`PUBLISHING_1` boundaries, and one mode-matched terminal. Any exact publication boundary latches outcome risk before mode and order validation, so malformed or premature boundaries still make later failure `OUTCOME_UNKNOWN`. The supervisor retains the raw child before structural validation and applies bounded cleanup only through proven callable handles. Supported resources are disconnected, closed and unreferenced.

The isolated child first requires the current operator-trusted profile and wallet-free readiness. It derives the recovery-state sibling from the exact current-directory generation and opens that state before the matching wallet workspace. Before wallet contents are read on a fresh attempt, it requires two equal complete pending snapshots with exactly one positive native ZNN send and one positive native QSR send to the protected account-zero address, then two zero-unconfirmed observations. The preserved legacy state name or the generated sibling with the identical suffix is durably armed once. That directory must be canonical, current-user mode `0700` and ACL-free; one retained no-follow directory descriptor plus retained mode-`0600` single-link file descriptors bracket every read, exclusive create, rename, directory fsync and post-write identity check. Hostile same-UID code remains inside the namespace trust boundary. The child derives the protected account-zero key only, uses the lockfile-pinned SDK composite with its built-in PoW, rejects fusion, zero difficulty, zero nonce or any non-receive result, and persists the exact signed receive before publication. It publishes each receive at most once and requires exact node lookup plus Momentum inclusion before the next receive.

An unresolved publication failure, timeout, disconnect, exact-lookup absence or lifecycle uncertainty records an unknown state, poisons the isolated runtime and stops. Recovery never re-signs, republishes or replaces an exact persisted receive. For RPC-observed `publicKey` and `signature` only, it accepts either strict fixed-length binary values or the released SDK's canonical ASCII-base64 byte representation when both fields use the same representation, decode to the exact lengths and re-encode identically. Prepared blocks remain strict, and every other SDK-decoded semantic field must equal its persisted value. This does not claim rejection of every noncanonical raw RPC JSON spelling before SDK deserialization.

Two-record zero-exit recovery remains read-only and requires semantically equal, Momentum-included observations of both blocks. A one-record recovery is fresh-account-only: receive zero must have account height one and the protocol empty predecessor, must be semantically equal and included, and must still be the current frontier. Before any wallet read, it requires two stable snapshots containing exactly one distinct positive native pending send to the same receiver and two zero-unconfirmed observations. It strictly looks up both send blocks, recomputes their content hashes, requires confirmed positive native sends forming exactly one ZNN/one QSR pair, and bounds the remaining source's inclusion by receive zero's acknowledged Momentum. The second source hash was not persisted before interruption, so the stable single-pending selection remains the explicit operator-trusted identity boundary.

Every transition from included index zero toward index-one preparation must first win one shared atomic, exclusive, never-removed second-receive attempt record binding receive zero to the selected second source's canonical core metadata and declared hash. Recovery separately recomputes that hash from the looked-up source content. Fresh execution commits its stable original source-one snapshot before index-one preparation. Recovery first completes every stronger historical check, competes for the same record, and re-fetches the committed source before wallet access. A pre-existing record while index one is absent stops every later or concurrent invocation without wallet reopening or re-signing. The partial index-one block must be the direct height-and-predecessor successor of receive zero. Once index one is durably persisted, restarts are observation-only. Every ambiguity before or after the single index-one publication remains nonzero `OUTCOME_UNKNOWN` with no automatic retry. Fixed `COMPLETE` after partial recovery means the overall two-receive operation is complete, not that both receives were published in that invocation. The one-shot marker, private recovery record and second-receive attempt record are retained after every outcome. Fixed `COMPLETE`, `RECOVERED`, `OUTCOME_UNKNOWN` and `FAILED` output contains no endpoint, path, wallet, address, block identifier, signature or diagnostic. Funding receipt creates no Gate-B evidence and grants no preflight, run or publication authority; Issue #45 remains open.

### Mandatory operator stop rules

Automation must stop before creating or populating private role files, provisioning live ingress, or accessing or funding the disposable wallet. An identified human operator must supply and approve those inputs. Only after that operator-authorized provisioning may preflight validate protected input-file identity, metadata, generation, public configuration, RPC syntax, and output noncollision. Historical WSS preflight opens every protected role file, reads only the public config and buyer-RPC input, and does not read buyer-wallet or facilitator-RPC contents. Current-testnet one-shot preflight opens six protected files, including the fixed hostname source, and reads the public config, both RPC role files, authorization, and hostname source, but never reads wallet contents. Neither preflight performs network activity.

The source-only macOS wallet helper is prepared and tested before this stop, but its invocation remains an identified operator action because it creates real secret material. Its grammar remains exactly `create --workspace <absolute-private-workspace>`. The selected direct child of the current user's canonical Application Support root must be either the preserved legacy `zenon-x402-gate-b-wallet` directory or a generated sibling named with that prefix plus exactly 32 lowercase hexadecimal characters. There is no environment, default, alias or normalization fallback. The directory must already be empty, current-user owned, mode `0700`, ACL-free, non-symlinked and outside all Git-bearing ancestry. The supervisor and child independently validate the same selection and require the child current directory to equal it before invoking or consuming the SDK and entropy source. The supervisor uses empty arguments and environment, ignored standard streams, enum-only IPC, the exact workspace cwd and one bounded private bootstrap pipe; no wallet secret enters any process channel. Terminal settlement synchronously disables all protocol actions and detaches only supervisor-owned protocol listeners before reaping, making late control messages inert. Failure destroys that bootstrap before a bounded TERM/KILL reap. An absent close leads only to deterministic disconnect/unreference cleanup and generic quarantine failure, never success or a claim that hostile same-UID or root code, or a kernel-unreapable process, terminated.

The child reuses the unchanged private-workspace capability. That capability reconciles retained pathname and cwd descriptors, exclusively and durably reserves exactly `buyer-wallet.json` and `buyer-address.json`, verifies distinct zero-length mode-`0600` single-link inodes, and syncs both directory descriptors before the SDK loader or entropy source can run. Production then injects 32 cryptographically secure bytes into the pinned SDK's Zenon-compatible entropy constructor and derives account index zero only. Safe short-write loops and per-artifact syncs precede a final two-directory sync and complete path, cwd, inode, generation, mode, link and ACL revalidation. This is not all-or-nothing file-content atomicity. Only a conclusively pre-effect first-open failure may be retried: both fixed leaves are rechecked absent through the retained pathname and cwd descriptors, all workspace metadata, ACL and generation checks remain valid, and neither SDK loading nor entropy was invoked. That classification is internal and exposes only the same generic failure result. The first reserved inode, an ambiguous or failed absence proof, SDK loading or possible entropy, and every later failure close handles and permanently quarantine the workspace without unlink, truncation, overwrite or retry; retained restrictive residue blocks a later attempt before SDK loading or entropy. No marker or third artifact records an ambiguous failure that leaves no residue, so the operator must quarantine the workspace and must not retry after that generic failure. The separate address artifact is cryptographically public but operationally private and linkable, so it remains mode `0600` until the disclosure or funding gate. The identity-bracketed pathname ACL check cannot exclude hostile same-UID races, and no protection is claimed against hostile same-UID code, root, or external backup and synchronization services. JavaScript secret clearing remains best effort, and the child is not an OS network sandbox. Key generation is chain-neutral and performs no RPC, network selection, funding, signing, payment, publication or Git action. Helper success grants no authority for any later gate.

The offline one-shot input primitive is source-only and remains disconnected from runtime activation. Its trusted import-safe launcher accepts private values in memory and invokes the fixed CLI with only one literal operation argument. Canonical schema-v1 plaintext or schema-v2 current-testnet-WSS bootstrap JSON crosses inherited descriptor 3 as a four-byte big-endian length plus at most 8192 bytes and exact EOF; the fixed supervisor forwards it to one isolated child on descriptor 4. The supervisor requires both a valid request-correlated `READY` and the successful FD4 write/EOF callback before sending exactly one `EXECUTE`; a terminal or close cannot succeed without both conditions. Both requested environments are empty, standard input is ignored, standard output/error are bounded and fixed, and control IPC is request-correlated enum-only. Importing the launcher or CLI has no filesystem or process effect. Direct CLI use without the private descriptor fails; this is deliberately not a shell interface for endpoints, paths, reviewed digests, or acknowledgements.

The endpoint source is versioned by family. Schema v1 remains `{"kind":"gate-b-protected-endpoint-source","rpcEndpoint":...,"schemaVersion":1}` for canonical numeric public WS; schema v2 is `gate-b-current-testnet-wss-endpoint-source` and carries only the exact canonical WSS endpoint. The quick-tunnel supervisor creates only hostname source v2, which adds the complete canonical path-free quick-tunnel binding. Its durable parser/serializer and the single frozen hostname policy live in the pure input schema with no runner, SDK, filesystem, or network import. The quick-tunnel HTTP snapshot parser reuses only that pure hostname validator and returns frozen `{hostname}`; it cannot fabricate, omit, or make the durable binding optional. `PROVISION_ENDPOINT` selects only from the closed bootstrap generation and creates the matching endpoint source with exclusive open and durable sync; it performs no network operation or cross-family conversion. The hostname remains a bare lowercase, non-IDN, single-label `trycloudflare.com` name with no scheme, credentials, port, path, query, fragment, percent encoding, or control character.

The child delegates protected-file operations to one import-safe, network-free module that exposes only an opaque fixed-leaf capability for workspace validation, reservation, reads, writes, identity checks, directory sync, and closure. Production is macOS-only: any other platform fails before protected-file access or reservation. On macOS the actual current directory and supplied workspace must resolve to the same current-user `0700`, ACL-free, non-Git directory and both descriptors remain retained. Every fixed leaf is current-user `0600`, ACL-free, regular, single-link, no-follow opened, identity-bracketed, and device/inode-distinct. After every successful complete reservation set, the capability verifies distinct zero-length files and immediately syncs both retained directory descriptors before returning control. A successful initial directory sync makes the reserved names durable. `PREPARE` therefore reserves all four prepared outputs before its hook or any wallet/source open; `PROVISION_ENDPOINT` and `AUTHORIZE` cannot populate their reserved output before the same two-directory boundary. If that initial sync fails, restrictive residue remains in the running system but its persistence is indeterminate; no output byte follows, `PREPARE` has not opened any protected input, and the workspace is still consumed and quarantined with no retry. `PREPARE` validates account zero against `buyer-address.json`, derives distinct account one, and emits the exact one-atomic-ZNN current-testnet requirement, reviewed quick-tunnel `/paid` resource, 60-second payment timeout, listener 41000, 30000-ms RPC timeout, zero attempts/delay, and one-ms recovery elapsed limit. It emits preserved plaintext runner/RPC generations 2/2 or current-testnet WSS generations 3/3, validates prospective and descriptor-reread artifacts with the matching parsers and SDK, syncs files then directory, and invokes the merged clean-current-`main` raw-tree attestor before reservation and immediately before success. A failure after the successful initial directory sync leaves durable restrictive reservation names and consumes the workspace; it never creates or reserves authorization.

`AUTHORIZE` revalidates the four private sources and four prepared outputs, rederives account zero and account one, requires equal endpoint copies and payee/config binding, recomputes the matching versioned configuration digest, compares the separately reviewed exact lowercase digest, checks every frozen value and acknowledgement, recaptures the current revision, and runs the merged attestor before exclusively creating and syncing authorization v2 for plaintext or v3 for current-testnet WSS. The trusted in-memory controller obtains the reviewed digest only through its separate post-`PREPARE` review schema and passes it through the private bootstrap; no direct shell workflow is defined. Authorization binds the complete parsed configuration, endpoint, payment intent, run, profile, acknowledgements, and revision, but its schema does not bind the wallet inode or payer. A later same-user wallet replacement therefore remains within the private operational trust boundary. Byte-array and SDK-key clearing is best effort, while JavaScript strings cannot be reliably erased. Installed dependencies, local Git metadata, and precisely timed same-UID mutation remain trusted inputs. None of the three operations performs DNS, sockets, RPC, listener start, wallet creation, funding, signing, payment, evidence capture/publication, or Issue #45 closure.

The next source-only slice adds an import-safe quick-tunnel schema, launcher and supervisor plus one focused synthetic test file. It reuses the unchanged hostname schema and opaque private-workspace capability. It adds no CLI, dependency, package script, workflow or runtime registration. Import and test execution start no real process, listener, tunnel, DNS request, HTTP request, wallet, RPC, signing, payment or publication operation.

The private bootstrap fixes operation and schema versions, the canonical workspace and `cloudflared` executable, a lowercase SHA-256 pin, and one of two exact telemetry acknowledgement pairs. One pair accepts possible `cloudflared` error telemetry; the other records an operator claim that external egress control blocks Sentry. The implementation validates the pair but cannot inspect or prove firewall state. The launcher enforces macOS before fork, starts the absolute supervisor module with the selected private workspace as its exact current directory, carries the frame only on descriptor 3, requests an empty standard environment, ignores standard streams, and exposes only a frozen fieldless lease. The workspace is the sole private bootstrap value necessarily present in current-directory state; arguments, environment and IPC contain none of the private bootstrap. IPC is exactly `READY/START/ACTIVE`, monotonic `CHECK/CHECKED`, and `STOP/STOPPED`; it never contains private values or diagnostics. Checks are single-flight, a `CHECKED` send remains governed by the original check deadline, stop preempts a check with the next request identifier, and late responses fail closed without consuming another identifier. Successful closure requires matching `STOPPED`, clean supervisor exit and close, and bounded proof that its retained exact detached PGID is absent. Startup, check, and shutdown timers are fixed fail-closed deadlines, while exact-group reaping and the ten-minute hard lifetime supply the eventual lifecycle bound; no direct-child timeout alone proves descendant absence.

Before any tunnel-process effect, the macOS supervisor opens the existing private-workspace capability, exclusively reserves the hostname-source leaf, and syncs both retained directory descriptors. It never removes, overwrites or retries that source. It requires the fixed basename `cloudflared`, an exact current-user-owned mode-`0500` regular single-link executable, and a root-to-leaf canonical symlink-free parent chain with the mandatory current-user mode-`0700` anchor described below. One ACL-free proof runs under the fixed component and aggregate bounds and is bracketed by path and retained-descriptor generations; immediate pre-spawn, post-spawn, and later `CHECK` assertions are stat-only. Native Mach-O, stable byte, repository-pin, and exact version `2026.8.2` checks bracket the tunnel spawn. The accepted pin identifies the frozen official-release-derived bytes, but the local path and same-user namespace remain operator trust and this is not vendor authentication. The fixed direct command disables configuration, credentials, origin certificate, auto-update, prechecks and management diagnostics, sends all child output to `/dev/null`, uses no shell or PATH lookup, and gives the child only one process-owned empty `0700` directory as `HOME`, `TMPDIR` and current directory. The version probe and tunnel launch use `cloudflared` as child-visible argument zero; the attested absolute executable path remains solely their OS exec target and is absent from argument vectors, options, environments, IPC and captured output. The synchronous version probe's two-second timeout controls only the trusted direct leader, not arbitrary descendants; outer whole-group cleanup and the hard lifetime provide the eventual bound.

Metrics discovery invokes exactly `/usr/sbin/lsof -nP -a -p <pid> -iTCP -sTCP:LISTEN -F0pftnPT` through a bounded no-shell seam and accepts exactly one process-owned IPv4 `127.0.0.1` TCP listener not using the origin port. During the fixed startup deadline, absence of that listener or an exact `/ready` 503 with zero ready connections may be polled at the fixed gap. Any provisional response pins the child, metrics port, hostname, and connector identity; drift is terminal. Three equal ready observations bracket the one durable hostname-source write and reread before `ACTIVE`. Every later `CHECK` uses its own bounded poll budget: only same-identity listener absence or exact 503/zero readiness may precede recovery, and `CHECKED` is emitted only after a fresh exact 200/one observation equal to the pinned state. Malformed output, identity drift, timeout, backward clock movement, child change, or late completion fails closed. Direct numeric-loopback HTTP uses GET, no proxy, redirects, connection reuse or content encoding, and strict bounded `2026.8.2` `/quicktunnel` and `/ready` snapshots. The supervisor retains its tunnel spawn return before structural validation, attempts bounded direct-handle cleanup, and removes the runtime directory only after validated direct-child closure; an unvalidated return or closure uncertainty preserves quarantine residue. Independently, the launcher retains the detached supervisor's exact PID as the attested PGID, probes and sweeps only that negative group identity with bounded TERM/KILL escalation, and does not infer group exhaustion from supervisor close. Matching `STOPPED` plus clean exit and close therefore cannot resolve the public lease until the whole group, including hostile remaining descendants, is proved absent; failure-path closure also waits for the bounded proof attempt before rejecting.

This is a lifecycle guard, not confinement or availability proof. Root and same-UID actors, the operator-pinned binary, host kernel, Sentry policy, Cloudflare service, DNS and routing, clock and certificate roots remain trusted. `/ready` establishes only one currently reported edge connection; it does not prove public hostname propagation, route stability, origin reachability, protected-resource delivery or payment. The hostname may become stale after any successful check. The trusted in-memory controller retains the same lease and requests fresh readiness immediately before `PREPARE` and immediately before `AUTHORIZE`. It has no live-run entry point; a separately authorized future run slice must perform another fresh readiness check of its own. Each effect remains independently gated. The slice performs none of those effects, makes no live-evidence claim, and leaves Issue #45 open.

Its exact boundary is `src/gate-b-quick-tunnel-schema.js`, `src/gate-b-quick-tunnel-launcher.js`, `src/gate-b-quick-tunnel-supervisor.js`, `test/gate-b-quick-tunnel.test.js`, `README.md`, and this plan. Existing Gate-B source, settlement, runner, wallet, package, dependency and workflow paths remain unchanged.

This PR-A slice has an exact nine-path boundary: `README.md`, `docs/IMPLEMENTATION_PLAN.md`, `src/gate-b-public-ws-inputs-schema.js`, `src/gate-b-public-ws-inputs-launcher.js`, `src/gate-b-public-ws-inputs-cli.js`, `src/gate-b-public-ws-inputs-supervisor.js`, `src/gate-b-public-ws-inputs-child.js`, `src/gate-b-public-ws-private-workspace.js`, and `test/gate-b-public-ws-inputs.test.js`. It does not modify the live-evidence runner, facilitator worker, source attestor, wallet helper, package scripts, dependencies, or runtime activation.

The controller slice is source-only, default-off and local-test-only. `prepareGateBPublicWsInputsForReview` synchronously validates and snapshots one exact initial schema, captures the run name once, constructs one immutable quick-tunnel bootstrap and retains exactly one opaque lease. Its order is exactly `PROVISION_ENDPOINT`, tunnel launch, one fresh readiness check, one `PREPARE`, then `GATE_B_CONTROLLER_REVIEW_REQUIRED_RUN_NOT_AUTHORIZED`. `authorizeAndPreflightGateBPublicWsInputs` consumes one exact review containing an independently obtained family-specific configuration digest but no run name. Schema v1 requires plaintext-transport, payment, and publication acknowledgements; schema v2 requires WSS-payment and publication acknowledgements and rejects the plaintext exception. It then performs exactly one fresh readiness check, one `AUTHORIZE`, and the filesystem-only matching-family preflight, ending at `GATE_B_CONTROLLER_PREFLIGHT_VALID_RUN_NOT_AUTHORIZED`. Preflight adds no readiness call or wallet-content read. `executeGateBPublicWsInputsOnce` irreversibly consumes the distinct exact Phase-3 acknowledgement, performs one third fresh readiness check, and delegates once to the matching isolated one-shot runner. Every asynchronous dependency result is required to be an unmodified native Promise. Early, late, duplicate, malformed, cancelled or ambiguous RUN transitions quarantine with no replacement or second execution.

The next source-only slice adds the same-process operator coordinator and its fixed human front-end. The direct command is exactly fixed `/usr/bin/env -i`, the absolute current runtime and the audited front-end module with no arguments and an empty environment. A synchronous TTY eligibility check precedes every child. The outer then exclusively binds and verifies the fixed origin listener before directly starting and capturing a detached watchdog, detached origin guard and detached reaper, in that order. It writes only the captured watchdog PID to the guard and reaper in separate exact bounded private target frames with EOF and privately transfers the same retained listener to both; no process identity is accepted from IPC or exposed in arguments, environment, files, output or status. Payload-free holder `READY` covers only the child-observable complete target frame and EOF, adopted listener, denial handler and liveness handling. The outer separately joins both holder `READY` frames with both target-write callbacks and both handle-transfer callbacks before it sends the watchdog's dedicated private fieldless `START` and returns a frozen setup capability. Before `START` the watchdog reads no bootstrap bytes, opens no operational resource, starts no descendant and emits no operational milestone.

Before any operational descendant can start, the outer owns exactly `127.0.0.1:41000` as an exclusive IPv4 listener and verifies the actual bound address. It duplicates that same kernel listening socket, not three independent binds, to the reaper and origin guard while retaining its own copy. All three holders actively accept and return only the same fixed raw-TCP HTTP 503 denial with a zero-length body, `Connection: close`, `Cache-Control: no-store`, and no `Date`, `Server`, request-derived bytes, logging or application data. Every accepted connection is ended with the fixed denial; idle, failed or over-limit sockets are destroyed. This is an intentional pre-RUN loopback network effect; it does not read request bodies or protected records and has no wallet, payment or RUN authority.

Only after setup does a trusted controlling TTY accept three paste-only canonical JSON lines under temporary raw-mode echo suppression. The existing bootstrap and review phases and deadlines remain unchanged. Only after preflight does a third 120-second phase open; it contains only the fixed acknowledgement authorizing exactly one live testnet RUN now, and its irreversible attempt latch is set before parsing. Each canonical write follows its one-shot private OPEN acknowledgement. Editing controls and early, surplus, duplicate, stale, malformed or phase-confused input or barriers fail closed. Terminal recovery remains limited to the captured Node `isRaw` Boolean on normal completion, handled failure, `SIGINT`, `SIGTERM`, process `disconnect`, and raw-mode control-byte rejection or cancellation. There is no guarantee for `SIGKILL` or `SIGSTOP`, and no `SIGHUP`, `SIGQUIT`, or `SIGTSTP` handler is claimed. The PTY test proves no echo and captured Boolean raw-state recovery, not complete termios equivalence. Coordinator, reviewer, controller-only public-input operations, tunnel supervisor and runtime are non-detached and inherit the watchdog group. The watchdog, guard and reaper remain three detached siblings with separate liveness ownership.

The coordinator retains one controller capability and tunnel lease. After the mandatory pause it launches one fixed non-detached reviewer for at most 30 seconds. That independently implemented reviewer opens exactly seven protected leaves and never the wallet, validates all cross-file semantics, brackets final verification with source attestation, and sends only one bounded private digest result. The merged controller independently recomputes the digest during `AUTHORIZE`. A valid preflight emits `GATE_B_CONTROLLER_PREFLIGHT_VALID_RUN_NOT_AUTHORIZED` and remains non-authorizing. Only the distinct exact Phase-3 frame can attempt one RUN. Success can reach only pending independent verification before the coordinator stops and proves its tunnel and owned groups absent.

After reaper `READY`, cleanup ownership is serialized: the outer requests reaper cleanup and does not concurrently signal the watchdog group. Payload-free `ABSENT` is valid only after the reaper has proved ESRCH for that captured group. Reaper loss, timeout or false `ABSENT` makes the outer terminate and wait for the captured reaper group first, then probe and if necessary apply TERM, 500-millisecond grace, KILL and ESRCH proof to the captured watchdog group while the outer and guard retain the listener. Guard loss poisons the attempt while the outer and reaper retain their copies. If the outer and reaper are both gone, the guard becomes the sole cleanup authority, proves watchdog absence before releasing its copy, and otherwise remains protective residue. Normal clean closure proves the watchdog/tunnel group absent before any holder releases the listener, joins guard and reaper exit, closes the outer copy, and repeatedly proves the watchdog, reaper and guard groups absent. The proof covers the exact same-group reviewer, tunnel supervisor and runtime child, not arbitrary descendants.

After the outer has joined both holder `READY` frames with both target-write and handle-transfer callbacks, any surviving holder preserves fixed-denial port custody across any one- or two-holder loss before Phase 3. The last listener is not released on ambiguous watchdog absence. Every uncertainty produces fixed quarantine with no retry, replacement, unlink, overwrite, restart or resume.

Phase 3 is a bounded testnet-only close/rebind exception for exactly one disposable, minimally funded run; it is not the production listener-handoff design. After one fresh retained-lease check, exact request-correlated control IPC makes the guard and reaper close and acknowledge their listener copies, then the outer closes its copy once. There is no retry or reversal. Only after the existing facilitator exclusively binds the exact loopback port, public-route health succeeds, and the exact expected 402 is observed may lazy wallet opening, signing and the one payment proceed. A competing bind, a holder-release timeout, or a facilitator bind timeout quarantines before wallet access. If delivery of `ORIGIN_RELEASED` is ambiguous, the one-use latch remains burned and the sender quarantines with no retry or replacement, but the watchdog may already have consumed the message and downstream effects, including the exact payment, may already have occurred. Subsequent reconciliation is limited to that exact attempt and payment. The trusted host kernel, root and same-UID processes remain inside the security boundary; this deliberate availability gap removes guard fallback during the run, so crash recovery cannot preserve the earlier fixed-denial listener. A production design still must inherit or broker the same kernel listener with no close/rebind gap, coordinate and retire every active fixed-denial acceptor, and retain the guard as the 503 fallback on server loss. This exception is default-off, non-production, never publishes, and does not close Issue #45.

The previously merged coordinator base was confined to exactly nineteen paths: its prior sixteen-path working boundary plus `src/gate-b-operator-reaper.js`, the inherited-mode addition in `src/gate-b-public-ws-inputs-launcher.js`, and its tests in `test/gate-b-public-ws-inputs.test.js`. This origin-port correction changes exactly seven paths: `src/gate-b-operator-origin-guard.js`, `src/gate-b-operator-coordinator-launcher.js`, `src/gate-b-operator-reaper.js`, `test/gate-b-operator-coordinator.test.js`, `README.md`, `SECURITY.md`, and this plan. The fixture is synthetic, test-only and absent from production imports. It changes no dependency, package script, workflow, runner, wallet, quick-tunnel supervisor or runtime-registration path. Module imports are inert. Offline validation uses only synthetic processes and local loopback; it invokes no live tunnel, external-network endpoint, RPC, wallet, funding, signing, payment, publication or RUN effect. Owned byte clearing is best-effort and immutable JavaScript strings, including the operator-selected pin, cannot be zeroized. The slice is default-off, unreleased and unactivated, supplies no run authority, and leaves Issue #45 open.

The public-input launcher now owns one detached CLI/supervisor process group; the worker is explicitly `detached: false`. The launcher trap-safely retains the spawn return's safe own-data PGID before validating the rest of its shape. Success, later shape failure and runtime failure reconcile only that exact negative PGID and require final absence, so clean leader output with a surviving descendant fails and no unusable group identity is guessed. The public-input supervisor synchronously disables protocol actions on first terminal settlement, detaches only its listeners, and releases its private descriptor, timers and supported IPC/child handles without directly signalling that worker; the outer launcher alone owns bounded exact-group TERM/KILL reconciliation. The quick-tunnel launcher applies equivalent abandonment cleanup while retaining its existing independent whole-group proof. Late descriptor, IPC, send, disconnect, exit and close callbacks are inert. Duplicate, reentrant, cancelled, malformed, closure-faulted or partial-reservation controller paths stop once and await active-stage settlement and closure; only proved group absence without active-stage uncertainty yields `GATE_B_CONTROLLER_CLOSED_RUN_NOT_EXECUTED`, while active-stage uncertainty, group uncertainty or possible workspace residue yields `GATE_B_CONTROLLER_FAILED_WORKSPACE_QUARANTINED` as the first and only terminal result. There is no retry, unlink, truncation, overwrite, replacement tunnel/wallet or backward transition.

The exact controller and lifecycle-hardening boundary is `src/gate-b-public-ws-inputs-controller.js`, `test/gate-b-public-ws-inputs-controller.test.js`, `src/gate-b-public-ws-inputs-launcher.js`, `src/gate-b-public-ws-inputs-supervisor.js`, `test/gate-b-public-ws-inputs.test.js`, `src/gate-b-quick-tunnel-launcher.js`, `test/gate-b-quick-tunnel.test.js`, `README.md`, `SECURITY.md`, and this plan. No CLI, package script, dependency, workflow, runtime registration, wallet reader, funding/signing/payment/publication path or run API is added. Merging this source does not invoke or activate it, create a live tunnel, fund a wallet, run a payment, publish evidence, establish production security or complete Issue #45.

The canonical-artifact binding slice freezes exactly one repository-supported tuple: the evidenced `2026.8.2` macOS arm64 release, official archive asset and archive digest, fixed executable basename `cloudflared`, independently reproduced extracted-executable digest, native format, and exact version-output grammar. Unsupported, missing, floating, basename-mismatched, or byte-mismatched identities fail before spawn. The executable leaf must be current-user-owned mode `0500`. The verifier retains no-follow descriptors for a root-to-leaf canonical symlink-free parent chain: every directory must be root- or current-user-owned, ACL-free, and not group- or other-writable; a current-user mode-`0700` anchor is mandatory, and every descendant directory is current-user-owned mode `0700`. ACL absence is proved once for at most sixteen parents plus the executable, with a 200-millisecond per-call cap and four-second aggregate deadline, then bracketed by fresh path and descriptor generations; later pre-spawn, post-spawn, and `CHECK` guard checks are stat-only. This code provides no download, update, or fallback path. The runtime verifier proves byte identity, not acquisition history. The accepted identity is an official-release-derived byte identity, not proof of vendor signing, notarization, vendor authentication, reproducible upstream build, malware absence, or immutable release. The synchronous version probe's two-second timeout targets only the trusted direct leader; it is not an OS sandbox and does not prove arbitrary descendant absence. Outer whole-group lifecycle cleanup and the hard lifetime provide the eventual bound.

The quick-tunnel supervisor is the sole attestation authority. A private launch-bound `WeakMap` token cannot resolve before the exact captured non-detached runtime child is bracketed by identical immediate pre- and post-spawn path, generation, digest, and version checks. One-time binding resolution moves the token from `POST_SPAWN` to `CONSUMED`; only a verified durable source write and exact reread moves it from `CONSUMED` to `SOURCE_WRITTEN`. Retained readiness checks re-attest the same child and artifact. Pre-spawn-only, copied, frozen, cross-launch, duplicate, stale, replacement, restart, and drift cases fail closed. Retirement clears child, path, pin, generation, identity, and token links and deletes the token records. Same-process module plumbing remains trusted rather than an unforgeable JavaScript authorization boundary.

`PREPARE` copies the complete path-free block into preserved plaintext `runnerVersion` 2 or current-testnet WSS `runnerVersion` 3. The independent reviewer reopens source and configuration, validates every canonical artifact, telemetry, runtime-control, and persistence value, cross-compares them, and covers the complete configuration with its family-specific digest. `AUTHORIZE` repeats those reads and comparisons and records the digest and block in the matching `authorizationVersion` 2 or 3. One-shot filesystem preflight opens the fixed hostname source as its sixth protected file, accepts only one complete unmixed family chain, recomputes its digest, and rejects before success on every mismatch. It remains wallet-content-free and effect-free and can return only `GATE_B_CONTROLLER_PREFLIGHT_VALID_RUN_NOT_AUTHORIZED`, never RUN authority.

Telemetry policy records either accepted possible error telemetry or an operator assertion of external Sentry egress control; the code cannot verify firewall truth or telemetry disablement. Runtime policy binds only the observable non-detached runtime child of the retained supervisor and never asserts outer watchdog ownership. Temporary runtime storage has cleanup-or-quarantine semantics. The durable hostname source persists with the protected one-shot workspace beyond lease closure, so same-lease controller continuity does not bound file retention and no zero-persistence claim is made.

This is a strict cutover: hostname source v1, exceptional runner v1, authorization v1, and every old, mixed, missing, or partial tuple are rejected with no migration or in-place workspace upgrade. Replacement, restart, re-attestation drift, or rollback requires a fresh one-shot workspace while the affected workspace remains quarantined; v1 code must never open an accepted v2 workspace. The implementation is confined to exactly fourteen paths: `src/gate-b-quick-tunnel-artifact.js`, `src/gate-b-quick-tunnel-supervisor.js`, `src/gate-b-quick-tunnel-schema.js`, `src/gate-b-public-ws-inputs-schema.js`, `src/gate-b-public-ws-inputs-child.js`, `src/gate-b-operator-config-review-child.js`, `src/live-evidence-runner.js`, `test/gate-b-quick-tunnel.test.js`, `test/gate-b-public-ws-inputs.test.js`, `test/gate-b-operator-coordinator.test.js`, `test/live-evidence-public-ws-once.test.js`, `README.md`, `SECURITY.md`, and this plan. It adds no new RUN entry point, invocation, authority, effect transition, selector, or execution behavior; it tightens the existing exceptional RUN/preflight validation so only the bound v2 chain is accepted. There is no controller, launcher, watchdog, reaper, package-script, wallet, payment, or publication change. Import and offline validation perform no live effect; invoking the separately documented operator command may open the quick tunnel during `PREPARE` but still has no payment RUN, signing, payment, or publication authority.

The current-testnet WSS generation slice has exactly seventeen paths: `README.md`, `SECURITY.md`, this plan, `src/gate-b-operator-config-review-child.js`, `src/gate-b-operator-coordinator-cli.js`, `src/gate-b-operator-coordinator-schema.js`, `src/gate-b-public-ws-inputs-child.js`, `src/gate-b-public-ws-inputs-controller.js`, `src/gate-b-public-ws-inputs-schema.js`, `src/live-evidence-facilitator-worker.js`, `src/live-evidence-public-ws-once-run-child.js`, `src/live-evidence-public-ws-once-supervisor.js`, `src/live-evidence-runner.js`, `test/gate-b-operator-coordinator.test.js`, `test/gate-b-public-ws-inputs-controller.test.js`, `test/gate-b-public-ws-inputs.test.js`, and `test/live-evidence-public-ws-once.test.js`. It reuses but does not modify the quick-tunnel implementation; the preceding separate three-path readiness-hardening milestone updates only its schema, supervisor, and focused test. Neither milestone changes packages, lockfiles, wallets, journals, payment/resource semantics, source attestation, workflows, or remote state.

A successful `preflight` does not authorize `run`. Stop again before invoking `run`; a separate explicit human authorization permits exactly one payment. Preflight and run do not themselves authorize evidence publication.

For the historical WSS runner, if publication becomes `SUBMISSION_OUTCOME_UNKNOWN` or `SUBMISSION_ACKNOWLEDGED`, or a process crashes after submission is armed, reconcile only the byte-identical payment and never construct or publish a replacement payment. Both current-testnet one-shot families perform no automatic reconciliation; quarantine their payer and journal for manual independent resolution. Recovery, ambiguity, or crash remains nonpublishable, and Issue #45 remains open.

## Experimental prepaid service-credit lane

This lane separates funding latency from per-request service latency. One ordinary, independently verified Zenon/x402 exact settlement funds a bounded provider-local grant. Later API calls reserve and consume units from that grant without waiting for another Momentum. They are off-chain metered service uses, not additional Zenon transfers, independent x402 settlements, official `batch-settlement`, or a trustless payment channel.

Current implementation status:

| Boundary | Status |
| --- | --- |
| Immutable offers and deterministic server pricing | Implemented offline |
| Grant accounting, expiry, and revocation | Implemented offline |
| Idempotent request reservation, signed maximum cost, and pinned server cost | Implemented offline |
| Conservative execution and ambiguity lifecycle | Implemented offline |
| Single-host SQLite persistence and local cross-process writer serialization | Implemented offline |
| Exact synthetic mock funding-settlement verification and atomic grant activation | Implemented offline and isolated |
| Default-inactive provider-policy plus evidence-v1 / activation-record-v2 Zenon funding consumer contract | Implemented offline; concrete provider policy and authenticated producer not implemented |
| Pure injected-data Zenon funding-observer cursor and quarantine state contract | Implemented offline and default-inactive; no authenticated source |
| Domain-specific SQLite owner for one Zenon funding-observer state | Implemented offline and default-inactive; durable local internal consistency only |
| Single-process READY-to-grant composition owner | Implemented offline and isolated |
| Default-off listener-free Zenon durable HTTP lifecycle owner | Implemented offline, unmounted, and isolated |
| External-holder grant-descriptor proof handoff | Implemented offline, default-off, and listenerless |
| External-holder handoff HTTP/1.1 adapter | Recommendation A implemented in source and tests; unmounted and absent from active runtime graphs |
| Provider-attestor child protocol and one-operation client | Implemented offline and default-off; no deployed external attestor |
| Native development-attestor source | Journal, PKCS#11 signer policy, and macOS approval adapter implemented locally; production signing denied, test fixtures isolated, release unprovisioned |
| Explicit fixed-output service-credit functional demo | Implemented offline and opt-in |
| Explicit fixed-output service-credit loopback demo | Implemented locally, synthetic, opt-in, and unreleased |
| Explicit service-credit local scenario benchmark | Implemented locally, synthetic, opt-in, and unreleased |
| Authoritative live funding-settlement verification and activation | Not implemented |
| Client-key commitment and Ed25519 proof of possession | Implemented offline |
| Canonical already-signed client authorization serialization | Implemented offline and isolated |
| Fixed empty-body HTTP execution route and fixed JSON result | Implemented offline |
| Opt-in numeric-IPv4 local loopback transport owner | Implemented locally, unreleased, and isolated |
| Pure durable-execution transition contract | Implemented as an inert core; imported only by the opt-in SQLite CAS boundary |
| Durable SQLite execution-state CAS | Implemented offline and opt-in; no callback or runtime integration |
| Opt-in durable application-callback deadline and quarantine runtime | Implemented offline and isolated; unreleased, unactivated, and not HTTP-integrated |
| Client-key generation, recovery, and lifecycle | Not implemented |
| General protected-response persistence | Not implemented |
| Refund, redemption, and unused-balance policy | Not implemented |
| Distributed or multi-host coordination | Not implemented |
| Live activation or production deployment | Not implemented |

### Bounded external-holder HTTP adapter milestone

Recommendation A is implemented as `src/service-credit-external-holder-grant-descriptor-handoff-http.js`, a separate default-off and listenerless owner of the existing external-holder handoff. Its constructor has no defaults: it captures one fixed canonical HTTPS origin and selection, the clock and challenge lifetime, only the selection-aware privileged descriptor seam, two distinct bounded canonical origin-form ASCII request targets, and a synchronous admission callback. The returned frozen surface is exactly `handle` and `close`. It derives no authority, origin, selection, route owner, or descriptor choice from `Host`, forwarding fields, bodies, or route parameters and adds no package script, export barrel, CLI, listener, TLS, schema, dependency, or runtime wiring.

The HTTP boundary accepts only exact `POST`. Before admission it scans raw rather than normalized headers, enforces exported ceilings of eight raw-header pairs and 8 KiB including framing overhead, and rejects transfer coding, trailers, expectations, upgrades, content encoding, ambiguous connection fields, and duplicate or noncanonical framing. A challenge is exactly `Content-Length: 0` with no `Content-Type`; a redemption is exactly one canonical length from 1 through the exported 1,024-byte ceiling plus exact `Content-Type: application/json`. Admission receives only a new frozen operation-and-declared-length record and requires primitive `true` without Promise or thenable assimilation. One admitted handler runs at a time with no queue. It bounds and copies every chunk, accepts only a complete non-aborted exact-length stream, fatal UTF-8, canonical JSON bytes, and the exact nested redemption schema, and does not drain a rejected attacker body. Success and the one indistinguishable failure use bounded independently allocated JSON plus fixed private/no-store, `nosniff`, exact-length, close headers.

Close blocks admission and closes the internal handoff synchronously, returns one idempotent native Promise, and waits for the admitted response attempt. A callback-triggered close makes that request unavailable. An incomplete body can intentionally hold close pending, so a mounting server must own body/header deadlines, connection abort and destruction, HTTP parser and request-smuggling policy, connection and request-count ceilings, trusted-proxy rules, TLS and certificate lifecycle, and peer-aware quotas. The one-outstanding-challenge starvation risk remains: one unauthenticated caller can occupy the handoff challenge until expiry even though the adapter also serializes admitted requests. No public mounting is acceptable without those outer controls and an explicit shutdown order.

Focused tests cover inert import, exact configuration and private surface, canonical targets, raw-header and body ceilings, every framing class, malformed/truncated/overrun/fatal-UTF-8/noncanonical/duplicate/extra input, fixed non-reflective responses, admission failure shapes, reentrancy, one-in-flight behavior, close under completion and abort, proof failure, expiry, replay, and selection mismatch. The existing certificate-pinned localhost composition regression now mounts the separately imported outer controller around its internally owned source adapter while retaining funding, durable authorization, replay, owner/store reopen, a second request, wrong-pin rejection, and deterministic partial-body quiescence. Merging these source-and-test slices would provide only offline source-and-test evidence; it is not deployment or live evidence and makes no payment, finality, activation, live-use, denial-of-service-resistance, or production-readiness claim.

Before any mounting, rollback removes the adapter source, focused test, composition-fixture wiring, architecture assertion, and these documentation changes. After an experiment, stop outer admission first, call adapter close, abort any stalled outer-owned request under the outer deadline, prove the close Promise and sockets drained, and then remove the adapter. A fresh durable-owner lifecycle must construct a fresh adapter; an old instance never reopens. The adapter creates no persisted state, but rollback does not revoke, erase, or reconcile a durable grant created elsewhere.

### Default-off outer HTTP ingress lifecycle milestone

Recommendation B is implemented by the additive `src/service-credit-external-holder-grant-descriptor-handoff-http-ingress.js` controller. It is separately imported, source-only, default-off, unmounted and listenerless. Construction creates and exclusively owns one existing external-holder HTTP adapter so only the controller can supply that adapter's established admission callback. Construction and import create no listener, timer, file, TLS context, connection, wallet, store, signer, payment, publication or other live effect. The frozen surface contains only `handle`, `close`, and a frozen aggregate-only metrics snapshot; there is no package script, export barrel, CLI, active runtime root, schema, dependency, fixture credential, workflow or deployment change.

The listener passes a real Node request and response plus an exact frozen request-scoped context containing only an opaque symbol peer token and a narrow abort capability. The controller validates that context and exact HTTP/1.1 without deriving identity or authority from `Host`, forwarding or normalized headers, route data, or the socket. It delegates raw route/method/header/body framing to the unchanged adapter. Only after that adapter validates raw framing does the internal seam create a fresh frozen `{ peerToken, operation, declaredBodyBytes }` admission record. The synchronous trusted callback must return primitive `true`; thenable/proxy/throw and all other non-valid returns deny without assimilation, queue, retry, fallback, or body access. The controller also wraps the synchronous clock and selection-aware descriptor owner passed into the adapter. A genuine non-Proxy Promise returned by clock, descriptor owner, admission, schedule, cancel, or abort is denied, never awaited or interpreted as success, and permanently quarantines the lifecycle. Generic thenables, accessor-backed `then` properties, and proxies are not read or assimilated. Reentrant and concurrent work is rejected rather than queued.

Promise observation uses only module-captured intrinsics and descriptors. An ordinary same-realm Promise with no own constructor, including a non-extensible one, is safely observable directly only while the exact captured native prototype-constructor/species descriptor path is still current. A proven genuine non-Proxy Promise is also safely observable when it already has an own `constructor: undefined` or when its own constructor slot can be descriptor-shadowed with a temporary data `constructor: undefined` route and restored exactly before captured handlers are enabled. Observation never obtains `then` from the returned value, inspects constructor/species safety only through descriptor operations, invokes no custom getter, constructor, species, thenable, or settlement value, and treats neither fulfillment nor rejection as authority. The temporary descriptor is installed only on the proven Promise, handler attachment and exact synchronous restoration are separately verified, and any unprovable step fails closed.

Controller ownership is limited to post-admission body and response deadlines, invocation of the exact request's supplied abort capability, attempted-response versus proven `finish` and failed `close`/`error` accounting, eight fixed enum telemetry events, bounded saturating safe-integer aggregate counters, and ingress quiescence. It never logs or exports request-derived dimensions. Deadline publication stores the exact ticket in its owner slot before the supplied scheduler runs, tolerates a synchronous callback clearing that slot, attaches the returned handle only while that exact ticket remains active/current, and otherwise cancels the late handle without republishing it. Scheduler throws and native-Promise handles fail closed. A safely observable unexpected Promise stays in the private close-liveness set until either captured settlement handler runs. A genuine Promise whose observation route cannot be proved safe remains permanently in that set rather than receiving a fabricated rejection continuation or false settlement deletion; even if it later settles, close can reach only the fixed uncertain terminal through grace. Neither case can permit clean close or appear in metrics/events. Event-sink returns remain intentionally ignored and entirely uninspected, including native Promises and thenables; sink throws increment only the aggregate telemetry-failure counter. Saturation gates work and quarantines rather than wrapping a counter.

Abort is transport advice only, not rollback or proof that application/durable effects stopped. Every supplied synchronous function—clock, descriptor owner, admission, event sink, deadline schedule, deadline cancel, and request abort—is trusted, event-loop-blocking, and unpreemptable while it runs. Every producer is obligated to finish synchronously and never return any Promise, Promise subclass, poisoned or hardened Promise shape, Proxy, or thenable. The event-sink result remains deliberately ignored rather than inspected or contract-checked, leaving its owner responsible for any asynchronous rejection. For every other callback, safe attachment contains later rejection only when the descriptor proof succeeds. For an unobservable genuine Promise the controller preserves close liveness but cannot safely install a handler, cannot suppress a later process-level unhandled rejection, and makes no such suppression claim.

Close synchronously gates future work, calls the adapter's close operation immediately, returns one native Promise to concurrent valid callers, aborts admitted work, and waits for admitted work, response disposition, and tracked unexpected work. Invalid close calls cannot steal the canonical Promise. A close-grace scheduling failure immediately fixes the result as uncertain even when admitted work ignores abort. Close-ticket cancellation success is required before clean accounting; a throw or native-Promise cancellation result yields only the uncertain terminal counter/event and rejection, never both terminal outcomes. If termination cannot be proved inside the close grace, the lifecycle permanently returns one fixed close-uncertain failure. It owns no durable-owner, TLS-context, listener, or arbitrary-socket close authority. The operator owns TLS/private material and certificate lifecycle, Node parser creation options, production authority and canonical origin, SNI/ALPN and direct-listener/authenticated-proxy policy, peer authentication and token derivation, coordinated quotas, sockets/listener lifecycle, deployment, activation, and transport-absence proof. Exact HTTP/1.1 rejection is defense in depth, not a replacement for Node creation-time parser/TLS settings.

The existing certificate-pinned ephemeral localhost composition fixture now mounts the controller and retains only its genuine TLS/listener duties: strict Node parser creation flags and bounds, HTTP/1.1 ALPN, expectation/upgrade/`CONNECT`/client-error/TLS-client-error/drop handling, exact request-scoped peer/abort context creation, socket tracking, listener close, and real `IncomingMessage`/`ServerResponse` proof. Its composed outer-ingress raw-wire cases cover equal and conflicting duplicate `Content-Length`, length plus transfer encoding, malformed delimiters and line endings, oversized headers, HTTP/1.0, `Expect`, upgrade, `CONNECT`, and a pipelined second request without implementing a custom parser. Node parser rejection covers the malformed, oversized, and ambiguous-framing cases; the controller owns HTTP/1.0 rejection; outer server events own `Expect`, upgrade, and `CONNECT`; and routing/request-count controls own pipelining. Rejected traffic never reaches trusted controller admission. Focused tests additionally execute exactly 144 distinct deterministic cases from the Cartesian product of all six request orders for body completion, body deadline, and close; eager versus deferred adapter/owner completion; response `finish`, `close`, or `error` before response deadline versus response deadline before finish; and synchronous, normal, or failing close-grace scheduling. Separate focused cases exercise cancellation success/failure, synchronous body/response/close ticket publication, hostile constructor/species descriptors, temporary-descriptor restoration, exact non-extensible native Promises, permanently unobservable genuine Promises, and every invalid native-Promise return contract.

This milestone is only offline source-and-test evidence. It makes no authenticated-production-ingress, production, live-service, wallet, signer, payment, publication, activation, chain-canonicality, finality, release, deployment or readiness claim. Before mounting, rollback removes exactly this controller, focused test, the narrow composition/architecture test edits, and this milestone's README, security and plan text. After mounting, rollback stops outer admission, closes controller and adapter, invokes exact request abort for stalled work, proves listener and transport absence, and only then closes the durable owner. Every ambiguous durable or application state remains retained for explicit recovery; neither abort, close uncertainty, transport removal nor code removal is reconciliation.

### Bounded HTTPS ingress owner milestone

`BOUNDED_HTTPS_INGRESS_OWNER_V1` adds one separately imported, default-off transport-owner candidate in `src/service-credit-bounded-https-ingress-owner.js`. Import performs no I/O, timer, socket, bind, network-listener, Promise-hook, or other process-global observer operation. Construction is inert. Construction accepts one exact canonical HTTPS origin/authority, a closed distinct origin-form target allowlist for `POST`, one explicit generation record, thirteen explicit count/deadline/telemetry bounds, one exact frozen downstream `{ handle, close }` lifecycle, one exact frozen deadline runtime, and one exact frozen trusted HTTPS server factory. It rejects proxies, accessors, symbols, inherited or extra configuration, noncanonical origin/authority and target forms, absent bounds, defensive-range violations, and inconsistent socket/request and deadline relationships. It accepts no TLS material, material locator, bind setting, private hostname, environment input, server handle, dynamic route registry, or default operating number. Its exact deeply frozen surface is only `start`, `close`, and `snapshotMetrics`.

Only the first valid no-argument `start()` invokes the trusted factory. The owner passes a deeply frozen strict option record and exact frozen callbacks, validates the returned exact `{ listen, close, closeAllConnections }` capability before invoking `listen`, and accepts success only after the matching listening callback and successful cancellation of its start ticket. The factory result remains local until the exact start capability, Promise, ticket, phase, unsettled state, nonterminal lifecycle, and absence of permanent uncertainty are revalidated. Terminal close during factory execution makes callbacks inert and prevents the returned capability from being retained or invoked through `listen`; a valid local result is disposed best-effort only through its captured `close` and `closeAllConnections` operations, without changing the already fixed uncertain result. Immediately after start-ticket cancellation returns, the owner revalidates start settlement, permanent uncertainty, listener terminal/error state, and the exact `STARTING` phase before incrementing success or entering `LISTENING`; synchronous cancellation reentry therefore cannot overwrite listener failure or close. TLS 1.3, HTTP/1.1 ALPN, no client-certificate request, strict parsing, required Host, no duplicate joining, no nonstandard body writes, one request per socket, and every supplied header, handshake, request, idle, connection and request-count bound are executable inputs to the factory contract. Throw, invalid return, Promise return, callback-order violation, listener error, deadline, partial start, or cancellation uncertainty permanently quarantines that generation; there is no retry, replacement, or reopen.

The trusted factory remains a consequential boundary. It alone combines the options with operator-custodied server-authentication material, applies constructor and property-style Node limits including the frozen `handshakeTimeout` and `maxConnections`, registers every supplied callback on its documented Node event, owns pre-handshake streams and cleanup, and uses its already selected numeric bind configuration when `listen` is invoked. Factory invocation and capability calls are synchronous trusted code and cannot be preempted by a deadline while blocking the event loop. The configured origin and HTTP authority are SNI/Host policy only; neither they nor a `LISTENING` result are observed proof of the factory's actual bind origin. The operator must validate that implementation and separately establish DNS and WebPKI authority, origin-to-bind mapping, hostname ownership, firewall and proxy behavior, reachability, certificate lifecycle, and deployment configuration. Static source cannot prove these duties. The separately imported bounded native Node adapter below now implements this source seam from explicit constructor-supplied byte material and one fixed nonzero bind; provisioning, outer assembly, deployment acceptance, and activation remain outside the owner and adapter.

Node's documented TLS-server `connection` callback is a pre-handshake stream that does not provide the later TLS wrapper's ordinary socket events, while `secureConnection` and `tlsClientError` expose the TLS wrapper. No stable public raw-to-TLS mapping exists. The raw callback therefore performs lifetime bookkeeping only: it consumes one bounded connection start and increments a scalar count of outstanding accepted handshakes. It does not inspect or retain the raw wrapper, install listeners, create concurrent owner capacity, or attempt later correlation. Multiple handshakes may be outstanding, and failure/success outcomes may arrive in any order. This scalar is trusted-factory event accounting only; it is not raw/TLS identity, physical association, FIFO order, peer identity, or request authority. No undocumented `_parent`, `_handle`, internal symbol, address tuple, timing, or sole-pending rule participates.

`secureConnection` is the post-handshake ownership boundary. A first exact TLS-wrapper outcome consumes one outstanding unit, then creates and publishes one concurrent reservation for that exact secure socket before any observable socket capability access or listener registration. Reentry therefore sees occupied secure capacity. Socket capability capture, terminal-listener registration/removal, and exact destruction each run under explicit owner-held socket-lifecycle in-flight accounting. Setup revalidates terminal, lifecycle, concurrency, capability, listener, reservation, and collection ownership after capability capture and each registration boundary. A request observed before secure-state publication can seal and revoke only the exact pre-eligibility reservation for its own socket; it receives no body or downstream authority, interrupted setup cannot resume to eligibility, and another secure reservation is not selected or revoked. Only after secure tracking and both terminal listeners succeed is secure state cleanup-visible, and it remains request-ineligible during header-deadline installation. Failed or synchronously terminal tracking publishes no eligibility and remains bounded by terminal evidence or close grace. A first `tlsClientError` outcome consumes one outstanding unit when available, increments `tlsRejected`, and destroys only the exact candidate wrapper it receives; it never selects or revokes another reservation. The generation-lifetime weak outcome marker prevents an exact duplicate secure/error callback from consuming another unit. Unsolicited or excess terminal callbacks cannot underflow accounting, consume secure capacity, or reach downstream. Listener `close` is the trusted server's terminal evidence for unresolved pre-handshake accounting; without it, clean close is forbidden and the close grace bounds uncertainty. Concurrent owner capacity, tracked sockets, and owned requests begin only at admitted secure wrappers.

Both TLS policy fields must be exact own data properties; absent, inherited, or accessor-backed `alpnProtocol` or `servername` is rejected without invoking an accessor. A numeric origin retains the undefined, null, false, and empty SNI values only when one is present in that own data property. Secure state becomes eligible only after capability capture, both terminal-listener registrations, and schedule return, all socket-lifecycle and scheduler calls are no longer in flight, the ticket and socket, abort, lifecycle, reservation, and collection ownership are unchanged, and the captured ALPN/SNI descriptors and values remain exact. A request during capability or listener setup terminalizes only its exact pre-eligibility reservation; later setup reentry is rejected without body access or downstream invocation. None can later authorize, and a failed or synchronously fired deadline permanently precludes clean close. Only an accepted secure socket receives a descriptionless symbol and one frozen no-argument abort closure, and only the real Node request/response and exact frozen `{ peerToken, abort }` reach downstream. The token differs after reconnect and is never derived from an IP address, forwarding or certificate fields, request data, payer, wallet, holder, grant, or payment. It proves one-use same-process admission capability, not peer identity. Exact raw Host must occur once and equal the canonical authority. Unsupported targets/methods, HTTP/1.0, duplicate or excessive headers, transfer encoding, expectations, upgrade, `CONNECT`, parser/client/TLS errors, drops, idle sockets, and pipelining fail before downstream, using one fixed unavailable response only when parsed framing permits and exact socket destruction otherwise. The outer request boundary rejects Proxy and accessor-backed security-relevant request fields without invoking attacker-controlled getters. It retains exact data descriptors for socket, method, target, HTTP version, and raw headers plus the canonical Host/framing array. Before any response method can affect admission, the request lifetime start is consumed and tracked request and handler concurrency ownership is published. Reentry during response registration, deadline scheduling, or `setHeader` sees that reservation and is rejected without a body read or downstream invocation. Response admission also retains false `headersSent`, `writableEnded`, `finished`, and `destroyed` state through exact own data descriptors or the captured native `OutgoingMessage` accessor route backed by exact safe own data. Accessor or descriptor ambiguity fails closed without invoking caller accessors. Finish, close, and error settlement listeners are installed before `setHeader`. The complete request, response-terminal, socket, descriptor-bound TLS snapshot, deadline, lifecycle, and ownership gates repeat after the observable header write, and the same full gate remains adjacent to downstream entry with no observable callback between them. The request whose own final lifetime reservation seals acceptance remains eligible; synchronous reentry cannot displace it or become a second authoritative request. Module-owned `Set`, `Map`, `WeakMap`, and `WeakSet` membership, mutation, size, and traversal dispatch uses operations captured at module evaluation through the captured `Reflect.apply`. That narrow rule makes the module's own collection use deterministic after injected callbacks; it is not isolation from arbitrary same-process code, pre-import poisoning, module replacement, workers, or another process. Global concurrent and lifetime socket/request budgets have no queue and never read a rejected body.

The outer owner bounds handshake, header, whole downstream-operation/response, idle, start, and close phases. It does not take over the existing controller's post-admission body/response/close deadlines and does not extend them; the whole-operation deadline is an outer containment ceiling. Downstream `handle` receives an exact frozen `{ success, failure }` completion as its final argument, downstream `close` receives one as its sole argument, and both calls must return exactly `undefined`. These owner-created capabilities, rather than caller returns, are the only downstream lifecycle authority. A synchronous completion remains provisional until the call returns normally with exact `undefined`; callback then throw or callback then non-undefined return fails closed. Each capability is one-shot and bound to one request or close generation. Duplicate same-result calls are inert, an active conflict fails closed, a missing completion is bounded by the request deadline or close grace, and deadline, close, and terminal detachment sever the callback cell so retained functions become inert. The owner never reads or invokes a returned `then`, constructor, species, or accessor, never assimilates a thenable, and installs no Promise hook or process-global rejection observer. A returned Promise, thenable, object, or other value cannot authorize handler success or clean close. Best-effort cleanup ignores returned values and infers no lifecycle success; trusted same-process producers remain responsible for containing any rejection they create. Contract violation, unobservable work, deadline-runtime failure, or cancellation uncertainty permanently precludes clean close.

Close gates admission first, invokes downstream close, stops acceptance, detaches every active handler completion cell, issues advisory aborts, records failed handler settlement in owner accounting, performs exact owner-admitted secure-socket destruction, cancels owned phase timers, and waits for downstream clean settlement, listener non-listening closure, zero outstanding accepted-handshake accounting, empty secure-state collections, zero sockets, requests and handlers, no raw callback still updating lifetime accounting, and no uncertainty. It does not await or observe a returned/native Promise; retained producer work may continue externally, while its detached completion capability is inert. The owner does not observe pre-handshake cleanup directly; listener `close` is the trusted factory/server evidence that clears unresolved scalar accounting. An owner-held close-setup gate covers the entire shutdown sequence. Each published server `close` and `closeAllConnections` invocation and each observable socket capability/setup/destruction operation also holds a dedicated in-flight reservation before invocation through return or throw validation. Synchronous reentry cannot publish terminal state early; server throw or non-`undefined` return latches uncertainty before release, and nested socket operations cannot underflow their counter. This also covers `close` invoked by budget exhaustion outside explicit owner close. Every unsuccessful handle cancellation, including cleanup callers that do not otherwise consume its Boolean result, permanently quarantines the generation. A trusted scheduler or cancellation call remains an in-flight lifecycle operation during synchronous reentry and cannot be mistaken for quiescence. Immediately after close-ticket cancellation, terminal and permanent state are checked again before `closeClean` or `CLOSED` can be committed. Valid callers share one native Promise; invalid calls cannot steal it. Exactly one clean or uncertain terminal metric is possible. The outer owner deliberately does not close the durable owner, store, payment, signer, attestor, publication, RPC, or WebSocket lifecycle. Abort and transport absence are not rollback, payment reconciliation, or exactly-once proof.

Both clean `CLOSED` and fixed `CLOSE_UNCERTAIN` use the same ordered terminal boundary: publish canonical terminal and admission-closed state, make external callback dispatch inert, deactivate retained abort cells, detach only safely releasable owner-held lifecycle and tracked-work references, clear scalar handshake accounting, freeze terminal metrics, and then settle the canonical close Promise exactly once. Before removing an active handler from owner accounting, close detaches its completion cell, issues abort, and increments both `handlersSettled` and `handlerFailures`. It installs no Promise reaction and waits for no returned operation. Retained external producer work may continue, but its detached completion functions are inert and cannot revise owner state. After settlement, the observable stage attempts only already captured timer cancellation, listener removal, and secure-socket destruction. Cleanup failure, invalid return, throw, or synchronous reentry cannot rewrite the terminal result, update frozen metrics, produce a false `CLOSED`, or leave the settled Promise pending. Zero tracked sockets, requests, handlers, and owned timers prove only release of those owner-held references and accounting, not termination of external operations, sockets, callbacks, or foreign emitters.

Metrics are one fixed phase enum and bounded aggregate safe-integer counters/gauges only, with no labels, exemplars, timing samples, origin, host, target, peer, socket, certificate, request, header, error detail, or diagnostics. The direct server-authenticated topology plus global limits is sufficient only for a bounded first pilot. It provides no pre-body peer identity, per-peer quota, reconnect resistance, fairness, availability, abuse resistance, multi-process coordination, or production readiness. Client-certificate and authenticated-proxy topologies remain deferred mutually exclusive options if stable pre-body identity becomes required. Every operating number is operator selected; synthetic values are not measured-safe recommendations.

Focused fake-runtime tests cover construction inertness, exact hostile configuration, strict option/callback delivery, start and close uncertainty, selective post-start cancellation failures, scalar out-of-order TLS outcomes, exact-candidate TLS failure, secure reservation ownership and rollback, server/socket shutdown-operation reentry, pre-eligibility request sealing, start/close cancellation reentrancy, protocol and budget gates, token nonidentity, hostile caller-return non-observation, operation-bound completion capabilities, response/handler settlement, privacy telemetry, synchronous callback races, late callback detachment, and zero residual owned work. The real-Node composition test replaces its former duplicate listener/parser/socket/close owner with this source while retaining synthetic server-authenticated TLS without a client certificate, wrong-pin failure followed immediately by strict raw-wire ingress cases, partial-body quiescence, funding challenge, holder handoff, active service, durable replay, reopen, unresolved-pre-handshake close ordering, and ordered outer-before-durable cleanup. A restricted runner that denies local bind cannot execute this fixture; that environmental denial is not composition evidence. This is merged-source and offline-test evidence only, not a release, deployment, activation, public-hostname observation, or live result. The milestone changes exactly seven paths, adds no dependency, package script, CLI, schema, environment switch, background start, or active import edge, and rolls back by removing its source/test plus only its composition, architecture, and three documentation edits. Removing transport cannot reconcile durable ambiguity.

`BOUNDED_NODE_HTTPS_SERVER_FACTORY_V1` adds `src/service-credit-bounded-node-https-server-factory.js` as the one separately imported native implementation of the existing ingress factory contract. The native adapter is invoked once only by the bounded ingress owner's later `start`; import and construction create no server or listener. Construction accepts one exact frozen `{ bind, tlsMaterial }`: an exact host, nonzero port, `exclusive: true`, and ordinary non-Proxy Buffer containers for `key` and `cert`. Proxy rejection precedes Buffer classification and prototype access. Captured typed-array internal-slot getters bound the bytes, and captured typed-array set copies them into independently allocated adapter storage without traversing caller-owned length, byte-length, offset, backing-buffer, constructor, species, or conversion properties. Each input remains under a 1 MiB source bound. That number is only a defensive contract limit, not a measured-safe recommendation, accepted key-strength profile, or production material policy. Request data, `Host`, forwarding fields, socket data, environment values, and filesystem discovery cannot alter this closed constructor authority.

One factory invocation accepts only the ingress owner's exact frozen strict option and 15-callback records. It constructs Node's HTTPS server with the captured byte options, applies and verifies `maxHeadersCount`, `maxConnections`, `maxRequestsPerSocket`, `headersTimeout`, `requestTimeout`, and `keepAliveTimeout`, applies the fixed idle timeout, and registers request, raw connection, secure connection, expectation, upgrade, `CONNECT`, client/TLS error, drop, timeout, listening, error, and close forwarding exactly once. Error and close evidence are armed before capability publication. The result is only an exact frozen `{ listen, close, closeAllConnections }`; each operation dispatches through a captured native function and synchronously returns exact `undefined`, never the native server or another lifecycle authority. No PFX, passphrase, engine, arbitrary option, trace/keylog hook, dynamic SNI context, precreated secure context, loader, file, environment default, rotation, address inspection, or test hook is accepted.

Definite native construction failure clears the private copies and publishes no capability. A later setup failure first detaches forwarding, removes nonterminal listeners where possible, invokes only captured server-local `closeAllConnections` and `close` cleanup best-effort, clears adapter references and copies, and returns one fixed private failure; cleanup is not clean ingress-close evidence. Capability close publishes a close-requested gate before native close, preventing first-listen or duplicate native-close reentry. That request does not detach or suppress the callback table: until actual listener `close`, already delivered connection, secure-connection, request, timeout, error, and related events continue to the unchanged ingress owner for its own admission, destruction, and accounting decisions. Actual listener `close` is terminal evidence: terminal state, callbacks, the mutable server reference, and adapter-owned copies are detached before the supplied close callback is invoked, making close-callback reentry inert and preserving a single close notification. Native `error` alone is nonterminal. A constructed factory abandoned before its one invocation has no disposal surface, so release depends on dropping that factory and eventual runtime reclamation. Buffer clearing is reference hygiene only and cannot prove cryptographic zeroization, absence from native TLS or allocator memory, crash-state absence, or erasure of caller-retained input. Native creation and dispatch primitives are captured at module evaluation inside a cooperating trusted-process boundary; later built-in export synchronization cannot replace the already captured creator, while pre-import poisoning, module replacement, workers, and arbitrary same-process mutation are not contained.

The adapter owns no router, durable composition, handoff controller, store, preexisting application work, outer deadline, material provisioning, issuance, custody, rotation, DNS/WebPKI authority, origin-to-bind validation, firewall/proxy policy, public reachability, deployment, or activation. The complete outer startup/shutdown assembly remains test-owned and preserves ingress/router close before durable/store close. The composition fixture generates disposable local material, releases an ephemeral loopback reservation, then constructs the fixed-port adapter; the release-to-listen interval is a test-only allocation race with no wait, retry, fallback, or production discovery claim. Focused tests cover exact configuration and surfaces, material-copy drift, one-use creation, native policy and event forwarding, synchronous capability returns, definite construction failure, test-owned interrupted setup, clean close, occupied-bind missing-terminal uncertainty, late detachment, and the real ingress contract. Architecture tests prohibit active imports, package mounting, process-global observers, async hooks, material loaders, extra native authority, and importers other than the focused test and explicit composition fixture. The seven-path rollback removes this source/test and restores only the fixture, architecture assertions, and three documents; it cannot reconcile any durable or externally retained work.

`DURABLE_HTTPS_ROUTER_V1` extracts the remaining test-local downstream route and Promise-to-completion bridge into `src/service-credit-zenon-durable-https-router.js`. The module is separately imported, source-only, default-off, unmounted, listenerless, and inert. Its frozen surface is exactly `{ handle, close }`; neither operation returns a Promise or any value other than exact `undefined`. The exact frozen constructor policy captures three distinct canonical targets, a closed three-value phase reader, a narrow synchronous funding-challenge operation with one copied frozen selection/resource record, a narrow durable handler, and a narrow handoff `{ handle, close }` lifecycle. It performs no import-time or construction-time call and adds no listener, server factory, TLS material, bind, socket, timer, file, store, wallet, signing, payment, publication, RPC, HTTP client, environment option, CLI, script, schema, dependency, workflow, deployment, or active import edge.

Routing is domain-specific and closed. The service-credit target is bodyless-unavailable in `UNAVAILABLE`, emits the existing fixed funding challenge in `CHALLENGE`, and delegates to the captured durable handler only in `ACTIVE`. The exact challenge and redemption handoff targets delegate only in `ACTIVE` and otherwise emit the existing fixed private/no-store handoff-unavailable response. All other targets and phase values fail closed. The request target is read only from the exact outer-admitted own data property and its descriptor must remain unchanged across observable phase and challenge work; no `Host`, forwarding field, socket datum, route parameter, body, payer, wallet, holder, grant, proof, signature, key, or request identifier selects authority. The real fixture supplies frozen narrow mount operations for its current durable and handoff generations, but those constructor-captured operations—not request input—remain the only route authority. There is no dynamic registry or arbitrary middleware.

The bridge accepts only a genuine non-Proxy Promise with the exact captured same-realm native prototype. It uses the module-captured `Promise.prototype.then` data value and captured `Reflect` descriptor operations and never obtains or invokes a returned `then`. Promise subclasses, Proxies, generic thenables, arbitrary objects, throws, and non-Promise returns fail closed. Own hostile constructor/species routes are not invoked. An unchanged ordinary native route is used directly; post-import global constructor/species drift can be bypassed only for an extensible Promise with no own constructor by installing, verifying, using, and synchronously removing an own data `constructor: undefined` descriptor. Failed installation, attachment, or exact restoration is permanent uncertainty. Both reactions are attached together, return values and settlement values have no authority, and a safely observable rejection is contained before producing the fixed route response and outer failure. A genuine but unobservable Promise is never claimed settled; clean close is forbidden. This narrow hardening does not cover pre-import poisoning, module replacement, workers, or arbitrary trusted same-process code.

Each exact frozen outer completion is consumed once and remains operation-bound while its callback executes. Synchronous response or completion reentry therefore cannot overtake active accounting. Fulfillment signals success once; rejection, contract violation, response failure, invalid phase, reused capability, or request-completion callback throw signals failure where possible and permanently gates new work. `close` gates handles first, invokes the captured handoff close once, then waits for all safely observed request operations and that close. Valid concurrent close invocations receive the same fixed clean or uncertain result through independent one-shot cells. The router never closes the durable composition, stores, TLS state, listener, or sockets, and owns no deadline. The bounded HTTPS owner's existing whole-request and close-grace deadlines remain the containment boundary; retained producer work may continue after outer detachment but its completion capability is inert. Clean router close proves only clean settlement of safely observed router-owned work, not durable rollback, external termination, payment reconciliation, or exactly-once effects.

Focused tests pin exact configuration/surface, construction inertness, every route and phase, fixed unavailable responses, native fulfillment/rejection, non-extensible Promise observation, hostile return shapes without `then`/constructor/species invocation, unobservable-Promise uncertainty, duplicate/conflicting/late settlement, close ordering and idempotence, in-flight drain, close/handle reentry, callback-triggered reentry, and post-import Promise/Reflect/Object poisoning. Architecture tests prohibit Promise hooks, async hooks, process-level rejection observers, timers, listeners, active roots, scripts, and any importer other than the focused test and explicit composition fixture. The real TLS fixture removes its local bridge/router/close functions and imports this module together with the bounded native Node HTTPS adapter while retaining disposable material generation, fixed-loopback-port reservation, outer assembly, and all strict raw-ingress, wrong-pin, handoff, durable replay, reopen, and close-order assertions as test duties. The seven-path rollback is the new source and focused test plus the fixture, architecture test, README, security document, and this plan. This is offline source-and-test evidence only, not production, deployment, release, activation, live-chain authority, finality, or exactly-once proof.

The completed capability slice commits each grant to a canonical client Ed25519 public key under one domain and verifies proof of possession over a separately domain-separated request message. The signed request includes `maxCostUnits`; the model refuses a higher server-derived cost before reservation. The module accepts no private key or raw bearer preimage and exports only public commitment derivation, signing bytes for an external signer, and verification. It does not generate, recover, rotate, or identify the client key as a human, wallet, settlement, or chain identity.

The completed `createServiceCreditAuthorization` client-serialization slice accepts one exact plain-data minimal grant, complete request, and already-signed six-field proof. It captures them once, invokes the existing verifier, requires the exact verified grant, request, proof-version, request-identifier, and cost-ceiling binding, and emits only the canonical unpadded `ServiceCredit` authorization string. Successful serialization proves only cryptographic consistency among the supplied grant, request, and proof. The header payload contains only the six-field proof; it does not encode the method, route, body digest, or content type and cannot guarantee admission by a particular handler. It must accompany the identical out-of-band request context. The legacy handler and opt-in durable session enforce the same exact `POST`, route `service-credit.execute.v1`, empty-body digest, and `application/json` context through the shared non-authorizing `createServiceCreditHttpAdmission({ store })` boundary. That helper returns only frozen normalized admission decisions and no execution or response authority. A broader shared client-and-handler wire module is a possible later refactor; the current helper remains server-side admission only. The serializer accepts no signer, private key, key-generation hook, path, HTTP client, listener, wallet, RPC, settlement, or persistence capability. The authorization and its proof, signature, public key, grant identifier, and request identifier are bearer-like single-request credential material that must be redacted from logs, diagnostics, snapshots, and general persistence. Outside the disposable synthetic-only local test and demo boundary, carry them only over authenticated transport. The durable ledger still stores its documented privacy-reviewed grant and request identifiers as accounting keys, but not the authorization, proof, signature, or public key. Temporary UTF-8 buffers are cleared where practical; JavaScript strings cannot be reliably erased.

The completed standalone HTTP slice accepts only exact `POST /service-credit/v1/execute` with an empty body and returns only a fixed JSON result code. Construction fully validates one immutable durable grant-admission snapshot; a later grant requires handler reconstruction. Each handler permits 64 request starts per second and eight active executions with no queue. It verifies the signed proof and maximum cost, durably reserves and begins, invokes one application callback, then durably completes or conservatively retains ambiguity. The callback has no timeout, limits are not shared across processes or hosts, arbitrary response bodies are unsupported, and the handler is not network-mounted by the current CLI or `/paid` path. The isolated demo invokes it only with in-memory request and response objects. Later non-loopback exposure requires authenticated transport and log redaction.

The implemented schema-v2 model/store lane includes a schema-neutral unresolved-state admission guard over all requests in one ledger. Exact existing-request replay and conflict validation happen first. While any request is `EXECUTING` or `OUTCOME_UNKNOWN`, a new reservation or a distinct pre-existing `RESERVED -> EXECUTING` transition fails before clock, pricing, accounting, revision, or callback effects. That same boundary enforces the existing 100,000-request hydration ceiling before a new record can reserve units; a full ledger keeps existing replay and conflict handling but admits no additional record. The existing HTTP fallback returns its fixed private 503 response for that internal model refusal without exposing the reason. Reads and cached durable success remain available, and exact non-authorizing replay semantics do not change. Existing SQLite `BEGIN IMMEDIATE` serialization extends the guard only to processes sharing the same supported local ledger. The effective distinct-callback concurrency is therefore one per ledger; the handler's value of eight remains a separate upper bound. A stuck or legacy unresolved record may deny service indefinitely by design.

This brake is a conservative current-state prerequisite, not the later deadline/fence/generation implementation below. It adds no timer, `AbortSignal`, invocation fence, execution generation, durable seal, canonical reconciliation evidence, successor generation, worker or process isolation, multi-host guarantee, or production authority. The current raw privileged `reconcileRequest` can remove only the blocker it terminalizes under its existing external-evidence trust assumption and remains explicitly insufficient as the future private reconciliation API. Semantic rollback requires stopping service and proving that the ledger has no `EXECUTING` or `OUTCOME_UNKNOWN` request; removing the guard is not a reconciliation action.

### Inert durable-execution transition core

The implemented `src/service-credit-execution-contract.js` module is a standalone pure reference contract imported directly only by the opt-in SQLite store boundary described below. The current model, HTTP handler, composition owner, pre-existing demos, ordinary package commands, and active paths neither call its API nor initialize durable mode; only the separately named durable loopback demo below explicitly opts into the store surface, and every demo loads the pure core only transitively. Its sole inputs are exact bounded plain data: a caller-provided non-secret ledger identity, one immutable versioned policy, one capacity, complete request identity, a selected duration, explicit wall-clock samples, and canonical SHA-256 commitments. It performs no clock read, callback, timer, signal, randomness, environment, filesystem, SQLite, transport, or global mutation. It has no product timeout default. The selected duration must be positive and no greater than the supplied positive safe-integer policy bound; a possible 600,000-millisecond policy remains a later operational evaluation.

The pure state contains one deterministic initial generation only. `prepareServiceCreditExecution` binds the request, policy, execution identity, selected duration, start, and checked deadline in a non-authorizing `PREPARED` record. `fenceServiceCreditExecution` returns only explicitly named candidate state and execution data and never grants permission to invoke. Repeated evaluation of the same stale prepared snapshot merely reproduces that candidate. The store integration recomputes from freshly read state and conditionally persists the candidate; even its `APPLIED` fence receipt is not invocation permission. The separate opt-in callback owner below consumes only one freshly acknowledged applied fence in immediate process control flow and never exposes that ephemeral decision. For a live fenced execution, completion requires an explicit wall-clock sample: before-start regression or equality/advance beyond the deadline selects sealed uncertainty, while only `start <= now < deadline` can select success. Terminal replay needs no clock. Completion and uncertainty produce immutable winner candidates, and SQLite serialization plus expected-revision comparison selects one durable winner under contention. Success first remains success without sealing, while uncertainty first makes the execution unknown and seals the generation against new prepare or fence operations. Exact replays converge, changed bindings conflict, and request replay/conflict handling precedes seal and capacity decisions.

`recoverServiceCreditExecutions` never invokes work. It classifies a prepared record as definitely not invoked and release-safe within this reference state, while a fenced nonterminal record becomes unknown and globally seals the generation. The store integration realizes the no-invocation classification by privately driving the in-memory ledger through its conservative release sequence in the same transaction and persisting only final `FAILED_RELEASED`; it does not add public reconciliation authority. Terminal records remain terminal. Exact deadline equality is expired, forward movement cannot reauthorize, backward movement fails closed, and all deadline addition is safe-integer checked. Returned state is deterministic, detached, deeply frozen, JSON-safe, and capacity bounded. The module captures the specific security-relevant built-ins it dispatches through; isolated tests verify deterministic identity, selection, replay and conflict precedence, sealing, and fixed errors after post-import poisoning of those exercised intrinsics and inherited setters. This targeted hardening does not resist pre-import poisoning, module replacement, arbitrary trusted same-process code, workers, or operating-system observation. Structural hydration and deterministic hashes do not authenticate serialized state. The module stores no arbitrary result, error, evidence, credential, or secret and exports no reconciliation, generation-clear, successor, migration, or downgrade operation.

The pure-core milestone changed no schema, model, handler, composition, demo, package, or active path. The physical prerequisite then established a compound envelope and explicit safe migration. The SQLite CAS slice imports the core only within the store, persists atomic compound transitions, and blocks durable-mode legacy mutation bypass. The separate opt-in callback owner below adds injected monotonic scheduling, transactional deadline completion adjudication, advisory abort, and late-settlement draining without changing active HTTP, composition, demos, packages, or runtime paths. The opt-in durable HTTP session below now connects shared admission to that owner only for explicit synthetic construction. Remaining work separately covers active composition and authenticated transport, private canonical evidence authority and reconciliation, generation clearing and successor activation, then worker/process isolation and distributed ownership under their own threat models.

### Physical SQLite v2 migration prerequisite

New service-credit databases use SQLite physical format and `user_version` 2 while retaining domain state schema v2. The exact strict table remains one row with one text column, but its canonical value is a compound envelope with exactly `physicalVersion`, `revision`, `ledgerState`, nullable `executionState`, and `checksum`. Creation and physical-v1 migration write `executionState: null`. The domain-separated physical checksum commits to every compound field and detects accidental corruption, not hostile same-UID modification. The combined envelope has a deliberate byte bound in addition to ledger-state and record limits, so configured record counts are not reachability promises.

The public ledger boundary remains stable. `load()` continues to return the exact schema-v1 envelope projection `{ schemaVersion, revision, state, checksum }`, where `state` is the unchanged domain schema-v2 ledger and the checksum is the existing legacy projection checksum. `getMetadata()` continues to report that projection. A durable execution-only write still advances the shared compound revision and therefore the public projection revision and checksum even if ledger state is unchanged. HTTP, composition, both demos, the benchmark, package commands, active paths, and domain-model semantics do not change.

There is no implicit upgrade. Ordinary `openExisting` accepts only exact physical v2. `ServiceCreditSqliteStore.migrateExisting(options)` and the equivalent exported helper are the sole explicit physical-v1 migration surface. Under `BEGIN IMMEDIATE`, the migrator validates file identity, the exact SQLite schema, application and user versions, canonical one-row legacy envelope, legacy checksum, domain-state schema and invariants, and configured byte and record capacity. It accepts only physical v1 whose embedded domain state is already schema v2 and has no `EXECUTING` or `OUTCOME_UNKNOWN` request. The winning transaction preserves the revision and public projection, stores `executionState: null`, and atomically rewrites the row and `user_version`. A usable v2 store is returned only after a known-successful commit.

Operators must stop and quiesce every legacy process and handle that can use the physical-v1 store before migration. Rejection before commit leaves the legacy envelope and version unchanged. Commit-attempt or acknowledgement ambiguity closes and quarantines the attempted handle; callers must explicitly reopen the same database to discover whether v1 or v2 is durable and must not retry migration automatically. Two local processes serialize through SQLite, so one may migrate and every later contender observes an unsupported already-v2 source rather than performing a second rewrite. This serialization prevents partial or duplicate migration but does not coordinate or preserve availability for a legacy process racing the cutover; a pre-existing v1 handle fails closed after the transition. Corrupt, noncanonical, incompatible, capacity-violating, unresolved, or already-v2 input is never repaired, deleted, downgraded, or converted. Old physical-v1 code cannot read the migrated v2 file. Rollback therefore preserves v2 data for compatible code and never converts it back.

Physical-v1 migration remains an inert format prerequisite. It grants no callback or invocation authority and does not initialize durable execution. Once a later explicit durable initialization replaces null execution state, reverting to the earlier null-only physical-v2 implementation fails closed. There is no conversion back to null or physical v1; rollback means stopping users and retaining the database for compatible code.

### Opt-in durable SQLite execution CAS

The SQLite store now exposes seven explicit methods: a zero-argument durable snapshot; durable initialization; atomic request preparation; fence persistence; normalized-result completion; unknown-outcome marking; and explicit restart recovery. Initialization is one-way from null to one exact hydrated execution-contract-v1 snapshot, requires an expected compound revision, requires every legacy request to be terminal by rejecting `RESERVED`, `EXECUTING`, and `OUTCOME_UNKNOWN`, and validates capacity against the configured request ceiling. This clean cutover never adopts a legacy hold. It does not run automatically on create, open, migration, load, HTTP construction, composition, pre-existing demos, or ordinary package commands; the durable loopback demo calls it explicitly after mock activation.

Every durable mutation validates exact plain data before callbacks or writes, acquires `BEGIN IMMEDIATE`, rereads and validates the complete physical envelope, compares `expectedRevision`, recomputes the relevant pure transition from that durable snapshot, validates ledger/execution cross-links, and persists both states under one incremented revision and checksum. The caller cannot supply execution state, transition candidates, or result commitments. Receipts are detached, deeply frozen, and use exactly `APPLIED`, `UNCHANGED`, or `STALE`. A stale revision returns `STALE` without mutation or retry. Commit-attempt or acknowledgement ambiguity returns no disposition, closes and quarantines the handle, and requires explicit reopen.

The newly added store-local snapshot and cross-link helpers capture their `String` and `Set` references at import. This is not broader hardening of the pre-existing transitive model, pre-import state, module replacement, or arbitrary trusted same-process mutation.

Preparation accepts the existing reservation input shape plus a selected duration. In one transaction it creates a new reservation, advances ledger state to `EXECUTING`, captures the configured trusted wall clock once, and creates the matching `PREPARED` execution. It never adopts a pre-existing `RESERVED` request. Stale revision and exact replay or conflict precedence remain first; for new work an above-policy duration is rejected before the clock, while non-safe deadline addition is rejected after exactly one clock sample and before pricing. Fence and recovery likewise capture the configured wall clock once only after the revision check and while holding the writer transaction. A fence before the stored deadline advances to `MAY_HAVE_STARTED`; an exact-deadline or later fence becomes `NOT_INVOKED` and releases ledger value to `FAILED_RELEASED`. The store-private release helper drives the existing in-memory model through conservative unknown-plus-privileged-release transitions but persists only the final compound state, exposes no reconciliation method, and never stores an intermediate unknown.

Completion derives a domain-separated commitment from the exact normalized cached result and persists ledger `SUCCEEDED` with execution `SUCCEEDED`. Unknown-outcome marking persists both unknown classifications and seals the generation. One conditional winner resolves success versus unknown, and later replay returns `UNCHANGED`. Explicit restart recovery releases `PREPARED` work as definitely not invoked, maps fenced nonterminal work to durable unknown plus sealing, and preserves terminal records. Exact deadline equality is expired; backward wall-clock movement fails closed. The store performs no continuous or monotonic runtime timing.

The required cross-links are exact: execution `PREPARED` or `MAY_HAVE_STARTED` with terminal classification `NONE` maps to ledger `EXECUTING`; `NOT_INVOKED` maps to `FAILED_RELEASED`; `SUCCEEDED` maps to ledger `SUCCEEDED` and its recomputed cached-result commitment; and execution `OUTCOME_UNKNOWN` maps to ledger `OUTCOME_UNKNOWN`. Durable state contains no unlinked `RESERVED` request. Every execution has exactly one matching ledger request, every durable `EXECUTING` or `OUTCOME_UNKNOWN` ledger request has exactly one execution, capacity is bounded by the ledger record ceiling, and the current contract permits at most one nonterminal execution. Every reopen and transaction revalidates these relationships, canonical bytes, checksums, identities, policy, deadlines, result commitments, capacities, and compound byte bounds.

After durable initialization, the legacy `reserveRequest`, `beginExecution`, `releaseBeforeExecution`, `completeExecution`, `markOutcomeUnknown`, and `reconcileRequest` methods fail closed before clock, pricing, accounting, revision, or state effects. Reads remain available. Offer and grant administration may still update the same compound row only while preserving and revalidating execution state. A sealed generation rejects new preparation and fencing and has no public reconciliation or successor-generation escape. Even a freshly acknowledged `APPLIED` fence receipt is durable-transition evidence, not reusable invocation permission. The separate opt-in owner below may consume that one fresh result only in immediate process control flow. `UNCHANGED` and `STALE` are never authorizing.

The SQLite CAS slice itself added no callback, timer, monotonic clock, `AbortSignal`, listener, HTTP route, composition owner, demo, process hook, environment read, wallet, RPC, settlement, network operation, authenticated evidence, generation clearing, successor activation, or production behavior. It leaves the current runtime on the legacy null-execution path. The separate owner below is an explicit local caller through the durable session and the separately named durable loopback demo, but remains disconnected from active HTTP, composition, and runtime paths.

The code rollback boundary is the store implementation, its focused test, the two demo tests whose exact transitive-import guards now include the pure core, and this milestone's text in the three documents. Before rollback, operators must stop every store user. A physical-v2 database that was never durably initialized remains null-compatible with the earlier reader. Any database containing non-null execution state must instead be retained for compatible code; clearing that state, converting it to physical v1, or treating code removal as reconciliation is prohibited.

### Opt-in durable callback owner

The standalone `createDurableServiceCreditExecutionOwner({ store, execute, deadlineRuntime })` factory is inert at import and construction and returns only frozen `run` and `close` operations. It requires an already initialized compatible durable store and captures one exact trusted `monotonicNowNs`/`schedule`/`cancel` runtime; `schedule` returns an opaque handle and `cancel` must consume it and return exactly `undefined`. It never initializes, migrates, recovers, reconciles, or closes the store. A run validates and detaches one exact request first, reads its current compound revision, prepares once, and proceeds only from that operation's fresh `APPLIED` `PREPARED` result. It captures monotonic time immediately before fencing and invokes the captured application callback only from that operation's fresh `APPLIED` `MAY_HAVE_STARTED` result. The callback sees only a frozen execution identifier and advisory signal. The ephemeral invocation decision exists only in immediate process control flow and is never returned, persisted, encoded, replayed, or reusable. `STALE`, `UNCHANGED`, reopened nonterminal state, or a replayed receipt starts no callback and is never refreshed or retried.

The store's transactional wall clock is the durable completion adjudicator. The monotonic runtime only schedules a bounded, chunked in-process deadline from the fresh fence's remaining wall duration; timestamps from the two clock domains are never directly compared, and exact equality expires. The store can still select uncertainty after a wall-clock jump, suspension, delayed event-loop control, or regression. A deadline attempts one durable unknown transition, and only an applied or read-back-proven unknown dispatches advisory abort. Abort observation proves neither cancellation nor rollback. There is no `Promise.race` or hard-real-time claim. Synchronous throw, rejection, hostile thenable, malformed result, monotonic regression, or timer-control loss causes one conservative durable unknown attempt without retry or raw persistence. Success-versus-unknown still has one durable conditional winner; established matching success is reported as success when this run already invoked, while a mismatched cached result latches and reports no success. Either commit ambiguity requires explicit reopen and operator handling.

Each owner admits at most one run and performs no duplicate coalescing. Callback- or abort-context `run` or `close`, including cross-owner reentry in the same process, fails before lifecycle or store effects to avoid self-deadlock. Cooperating owners that share the supported SQLite ledger can start at most one callback after one fresh fence winner; this is not an exactly-once external-effect claim and is not distributed ownership. Unknown may return before a callback settles, but active accounting remains until late fulfillment or rejection is observed; late settlement performs no completion, retry, release, reconciliation, or reopening. `close()` is wait-only: it blocks new admission and waits for every callback started by that owner, terminal persistence, late observation, and proven timer cleanup. Shutdown itself never aborts or mutates durable state, and the normal deadline remains active. A callback that ignores abort and never settles may retain capacity and keep close pending forever.

This owner is opt-in, unreleased, and unactivated. Its only HTTP consumer is the unmounted synthetic session below; that session is constructed by the separately named durable loopback demo and exercised twice by the bounded pilot below, while all remain absent from the current `/paid` server, mounted handler and composition owner, benchmark, and active runtime. Explicit operator invocation of store restart recovery remains outside the owner and is deterministic state recovery, not authenticated canonical reconciliation. Rollback requires stopping admission and obtaining clean resolved owner closure. Any unresolved, unknown, active, ambiguous, or close-uncertain durable state requires retaining the database and compatible durable-execution code; clearing, releasing, downgrading, or treating code removal as reconciliation is prohibited. Active HTTP/composition mounting, authenticated transport and canonical reconciliation, successor generations, worker/process isolation, and broader measured load and capacity work remain separate reviewed milestones.

### Opt-in durable HTTP session

`createServiceCreditHttpAdmission({ store })` is the shared non-authorizing parser and admission boundary used by the existing handler and the separate durable session. It returns only frozen normalized decisions and exposes no execution or response authority. `createDurableServiceCreditHttpSession({ store, execute, deadlineRuntime, selectedDurationMs })` requires one already initialized durable store, privately constructs the reviewed durable owner, and returns only frozen `handle` and `close` operations. It owns no listener or store, does not mount itself, and never initializes, recovers, reconciles, retries, or closes the borrowed store. The server fixes the selected positive duration at construction within the persisted policy; HTTP input cannot choose or change it.

The supported construction order is synthetic funding verification and grant activation, explicit durable initialization, then session construction. Funding and administrative writers must remain quiesced while it serves because an unrelated global compound-revision update can make the owner stale and fail closed with unresolved work retained. Shutdown first stops and closes transport admission, then awaits session close, and only then closes the borrowed store. Closing the store underneath a live session is prohibited.

The shared parser preserves fixed private `400`, `401`, `404`, and `405` admission responses. A request-identity conflict or terminal `NOT_INVOKED` result maps to fixed `409`; busy, stale, recovery-required, unknown, closed, quarantined, ambiguous, and unclassified outcomes map to fixed `503`. A fixed `200` response is possible only for a strictly validated durable `SUCCEEDED` result or matching exact `CACHED_SUCCESS`. Responses remain private, no-store JSON and carry no internal diagnostic or retry instruction.

Exact replay returns the cached response without another callback, debit, or revision. Concurrent identical requests are not coalesced: a loser receives `503`, and a later exact replay may converge to the cached `200`. Disconnect neither cancels callback work nor proves its durable outcome and grants no replacement or retry authority. Session close is wait-only. Callback-context close rejects promptly, including while an external close is pending. An accepted external close waits for owned callback work and terminal persistence and may remain pending indefinitely if trusted work never settles.

The deterministic offline vertical test performs one synthetic mock settlement-to-activation flow, explicitly initializes durable execution, serves A, exact replay A, and B, closes transport then session then store, reopens the same durable state, serves cached A and B with zero new callback or accounting change, and then serves C. The final state contains three successful executions, three callback starts, six consumed units, and one available unit. Fault-boundary tests cover representative malformed admission, socket disconnect before and after durable success, prepare/fence/completion pre- and post-commit ambiguity, deadline unknown with late-rejection drain, callback-context and external-close races, unrelated compound-revision interference, and overlapping identical-request convergence.

Plaintext numeric loopback and bearer-like credentials are used only as disposable synthetic test material. Outside that boundary authenticated transport is required. SQLite coordination covers only cooperating processes using one supported local ledger; it is not distributed ownership and cannot guarantee exactly-once external effects. The session remains opt-in, unreleased, unactivated, and unmounted and performs no automatic recovery or reconciliation, successor activation, live x402, L1 settlement or finality, release, or production operation. Rollback requires stopped admission, ordered transport/session/store closure, and proof that no active, unknown, sealed, ambiguous, or otherwise unresolved execution remains. Otherwise retain the database and compatible code; never clear, release, downgrade, delete, or reinterpret unresolved state as rollback.

### Synthetic durable loopback demo

The separately named `npm run --silent demo:service-credit-durable-loopback` entry point is an explicit opt-in, unreleased, unactivated, unmounted synthetic scenario. Its zero-argument runner directly uses the mock settlement-to-activation adapter, creates a new physical-v2 store, explicitly initializes durable execution, and binds the existing durable HTTP session to the bounded numeric-IPv4 loopback transport. It does not use privileged direct grant insertion or the active composition owner, and it does not import or alter `/paid`, wallet, RPC, live settlement, or other active paths.

Phase one submits A, byte-identical replay A, and B in sequence. It proves two callback starts and durable successes, replay stability with no second callback, debit, or revision, and terminal accounting before closing transport, session, and borrowed store in that order. The runner then validates the recorded directory/database identity and exact single-file layout. Only this clean terminal-state reopen creates a new store, session, and listener. Cached A and B preserve revision and callback count; C completes the third durable execution. The final invariant is one mock settlement and activation, three callbacks and successful executions, six units consumed, zero held, and one available.

The selected execution duration is a fixed server-owned value within the durable policy. Funding and administrative mutation are quiescent while a phase serves. The shared admission/session mapping keeps exact private `400`, `401`, `404`, `405`, `409`, and `503` failures, while `200` requires validated durable success or matching cached success. Requests use new connections, follow no redirects, run sequentially, and never coalesce, retry, or replace work. The response deadline and durable callback deadline are safety bounds only, not measured results. A disconnect, malformed response, deadline unknown, commit ambiguity, incomplete validation, or close uncertainty returns fixed failure, authorizes nothing further, and retains the ledger. The demo provides no automatic recovery or reconciliation. Reopen demonstrates only clean terminal restart with new objects; it is not recovery from `EXECUTING`, `OUTCOME_UNKNOWN`, a sealed generation, or ambiguous closure.

The runner captures every store and mock settlement/activation method it dispatches at module initialization. It separately verifies the admission layer's live store-`load` method and the facilitator constructor's live verifier method before those existing seams can execute. This is a bounded defense against post-import replacement of the named prototype surfaces; pre-import poisoning, module replacement, and arbitrary trusted same-process mutation remain outside the milestone.

Each run creates one process-owned mode-`0700` directory and mode-`0600` plaintext database under the canonical temporary parent on a reliable POSIX local filesystem. Before reopen and deletion, it checks the captured user, canonical parent, directory and database identities, mode, inode, regular-file and single-link properties, and exact directory entries. Normal deletion occurs only after both phases close and final state validates, unlinks only the exact database, and removes only the verified empty directory without recursion. Detected replacement, symlink, hard-link, sidecar, identity drift, or cleanup uncertainty leaves state intact. If the final protected-string scan detects synthetic credential bytes in otherwise terminal identity-verified state, that exact owned state is removed but the run still fails. Same-UID hostile mutation outside these synchronous checks, kernel/storage compromise, and abrupt termination are not solved; interruption may leave the plaintext ledger.

The runner returns one deeply frozen fixed summary. Its CLI accepts no arguments and uses one fixed success line or one fixed failure line without diagnostics. A short descriptor write can emit an allowlisted partial prefix, so every stream from a nonzero run is invalid. Origin, port, path, identifiers, authorization, proof, signature, key material, response bytes, database content, timing, and diagnostics are excluded. Plaintext loopback remains permitted only for disposable synthetic credentials; authenticated transport is required for consequential credential use when same-host observers are not trusted.

This milestone makes no exactly-once external effects, distributed or multi-host ownership, authoritative settlement, wallet, RPC, blockchain, live x402, L1 finality, benchmark, latency, throughput, concurrency, release, activation, or production-readiness claim. Rollback is exactly eight paths: remove `src/service-credit-durable-loopback-demo.js`, `src/service-credit-durable-loopback-demo-cli.js`, and `test/service-credit-durable-loopback-demo.test.js`; remove `demo:service-credit-durable-loopback` from `package.json`; remove only this milestone's text from `README.md`, `SECURITY.md`, and `docs/IMPLEMENTATION_PLAN.md`; and revert only the corresponding durable-demo allowlist and wording assertions in `test/service-credit-loopback-server.test.js`. Rollback does not delete any retained database. Operators must first stop admission and prove clean terminal closure; any active, unknown, sealed, ambiguous, close-uncertain, or otherwise unresolved durable state must remain with compatible code.

### Synthetic durable local-load pilot

The implemented `npm run --silent pilot:service-credit-durable-local-load` command is a separately named, opt-in, unreleased, unactivated, unmounted synthetic-only pilot. Its sole public runner is zero-argument and starts exactly two same-process asynchronous invocations of the import-captured durable loopback demo before awaiting either. Each lane owns one of two independent durable ledgers, stores, sessions, listeners, mock settlements, and activations. There is no shared-ledger capacity experiment, worker or child process, new schema or dependency, active runtime import, timing capture, threshold, or production composition API.

The module-local latch admits one pilot invocation. Immediately after both fixed launch attempts, every accepted native lane Promise receives an exact immutable own constructor pin and then a module-intrinsic `await` observer before the post-launch integrity decision and any permanent quarantine. Every safely observed lane is drained before the pilot returns ordinary failure; it performs no retry, replacement, cancellation, restart recovery, reconciliation, or successor action. Failed pinning, broken scheduling integrity, an unobservable possibly started lane, or a never-settling child leaves both the operation and latch pending. Exact descriptor-safe validation accepts only the deeply frozen durable-demo summary, and captured dispatch prevents the exercised post-import intrinsic replacements from forging aggregate success. Pre-import module replacement and arbitrary trusted same-process code remain outside this bounded defense.

One verified aggregate records two mock funding settlements and activations, six application callbacks and distinct durable successes, 12 units consumed, zero held, two available, stable exact replay and clean terminal reopen, and verified cleanup in both children. It records no timing and is not a benchmark. It yields no TPS, latency, capacity, scalability, same-ledger concurrency, distributed ownership, exactly-once external effect, authenticated transport, live-chain settlement, release, activation, or production evidence. Authenticated transport, active composition, recovery or reconciliation authority, and broader measured load and capacity work remains future work.

The orchestration layer owns no ledger, listener, store, credential, or cleanup path. Each child retains its reviewed identity checks and deletes only its exact owned terminal-success state; unresolved or ambiguous child state remains quarantined with compatible code. Rollback is exactly eight paths: remove `src/service-credit-durable-local-load-pilot.js`, `src/service-credit-durable-local-load-pilot-cli.js`, and `test/service-credit-durable-local-load-pilot.test.js`; remove `pilot:service-credit-durable-local-load` from `package.json`; remove only this milestone's text from `README.md`, `SECURITY.md`, and `docs/IMPLEMENTATION_PLAN.md`; and revert only its closed package/import assertions in `test/service-credit-loopback-server.test.js`. Stop new pilot admission and await every started lane before rollback.

### Durable callback-deadline implementation and remaining gates

**Current behavior.** The active runtime still commits only the legacy ledger `RESERVED -> EXECUTING` transition before callback invocation and has no callback deadline, `AbortSignal` contract, durable owner wiring, or restart-time deadline control. Loopback request and shutdown bounds control transport resources only. They do not cancel application work, prove an external effect stopped, or change its durable classification. The opt-in store persists a cross-linked execution identity, selected wall-clock deadline, fence, transactional completion winner, generation seal, and explicit restart classification. The separate opt-in owner adds a bounded in-process monotonic schedule, advisory abort, and late drain. The opt-in session above connects shared HTTP admission to those layers only when explicitly constructed; the durable loopback demo constructs it, but no active composition, mounted command, or runtime does.

**Durable sequence and invocation fence.** The opt-in owner uses atomic store preparation to bind the execution identity, initial generation, exact policy, selected duration, restart-reconstructable wall-clock deadline, and ledger `EXECUTING` state. It then requires one freshly acknowledged `APPLIED` `PREPARED -> MAY_HAVE_STARTED` store transition before calling user code; neither the pure candidate nor any replay or stale receipt authorizes invocation. The injected monotonic runtime schedules only the current process and cannot reconstruct or adjudicate a deadline after a crash. Active completion is adjudicated again from one trusted wall-clock sample inside its writer transaction. Explicit store restart recovery remains outside the owner.

A failure proved to occur before any fence commit attempt may follow explicitly safe pre-invocation recovery. Once that commit is attempted, acknowledged, or ambiguous, crash or reopen must assume invocation may have begun, quarantine the work, and never automatically re-invoke or release it. Crash and commit-acknowledgement loss require coverage at every boundary around the execution transition, fence, and callback call.

**One durable winner and strict global quarantine.** Callback completion and deadline, disconnect, abort, shutdown, or lost-control uncertainty use conditional durable transitions so only one wins in the opt-in store and owner. `SUCCEEDED` committed before the uncertainty transition remains success; later response-delivery loss, disconnect, or acknowledgement loss neither downgrades the request nor quarantines its owner/store generation, and may affect only response recovery under existing semantics. `OUTCOME_UNKNOWN` or quarantine committed first blocks late completion, release, retry, and ordinary admission or reopen. If ordering or acknowledgement of either commit is ambiguous, preserve the last proven durable state, stop admission, reopen and read the store, and fail closed. Reopen distinguishes `EXECUTING`, `OUTCOME_UNKNOWN`, and `SUCCEEDED`. Explicit operator-invoked store recovery classifies a reopened fenced `EXECUTING` request as unknown and sealed without re-invocation or release; it is deterministic recovery, not authenticated reconciliation authority.

A post-fence deadline, disconnect, abort, shutdown, or loss of callback control quarantines the affected request, reservation and grant value, and the entire owner/store generation only when its uncertainty transition wins before any proven terminal success, or when ordering or commit acknowledgement is ambiguous. A durably proven earlier `SUCCEEDED` result follows the response-recovery rule above instead. Once sealed, the persisted gate stops all new callbacks, reservations, and reauthorizations for that generation, including unrelated requests, while tracking every callback already fenced or in flight in that generation. The opt-in SQLite store coordinates this gate across cooperating handles and processes sharing one supported local ledger; it does not provide distributed or multi-host ownership. A timed-out, ignored, or never-settling callback retains its active-execution and shutdown/close accounting slot until its promise settles. Returning an unknown result does not free capacity. The strict global latch prevents accumulation beyond the configured ceiling; if the callback never settles, ordinary service does not resume automatically. Both a persisted unknown and uncertainty while persisting it stop admission, but the latter requires reopen and readback before classification.

Reconciliation of one request cannot reopen a sealed generation while another fenced request is live, `EXECUTING`, `OUTCOME_UNKNOWN`, commit-ambiguous, or otherwise lacks a proven terminal reconciliation. The old generation stays sealed until every fenced request has a proven terminal classification and every associated callback has settled, or terminal evidence proves that callback cannot create any later effect. Clearing the gate and creating or activating a successor generation is one durable conditional transition. Partial, stale, conflicting, or concurrently losing reconciliation keeps the old generation quarantined.

An `AbortSignal` is advisory only. The owner supplies one fixed, input-independent string reason only after durable unknown is proven. Its observation, rejection, a timeout wrapper such as `Promise.race` settling first, or process termination cannot prove an external side effect stopped or was undone. Late success or failure is observed and drained without another durable transition, retry, release, or reopening; any later evidence-based reclassification belongs to the future bounded reconciliation boundary. The captured `execute` callback, `deadlineRuntime`, and abort listeners are trusted same-process code. They are not process-isolated by this owner, and exceptions thrown from abort listeners follow platform event-dispatch behavior rather than becoming guaranteed sanitized owner errors.

**Privileged reconciliation.** Idempotency capability alone is insufficient to release value or reopen service. Terminal evidence must establish either that the effect occurred and further execution with the same identity can only converge without another effect, or that no effect occurred and no still-live callback can create one later. Otherwise quarantine persists. Reconciliation requires a separate future private, privileged boundary, not the current raw-model `reconcileRequest` API. Its canonical bounded evidence record must be durably bound to the request, execution generation, deadline basis, invocation fence, decision, evidence type and digest, and versioned authority policy, without fixing final schema names in this plan. Exact replay converges; changed evidence or decision conflicts; concurrent attempts have one durable winner. It persists no arbitrary callback result, diagnostic or error text, bearer or credential material, or secret. Authentication, trust anchors, and key lifecycle for that authority are separate design work, so the boundary cannot be considered production-ready.

Shutdown continues to stop ingress and close the listener before store close. Callback-control or listener-close uncertainty preserves the SQLite ledger and prevents a clean-shutdown claim; it does not establish process-wide cancellation. Killing a process cannot prove an external effect stopped.

**Implemented coverage and remaining red gates.** Focused pure, SQLite, owner, admission, session, and integration tests cover deterministic identity, atomic preparation and fencing, transactional deadline equality and wall-clock movement, monotonic scheduling and chunking, success-versus-unknown race orders, fixed advisory-abort ordering, ignored abort, late fulfillment and rejection drain, explicit restart classification, commit ambiguity, local contention, global quarantine, retained capacity, synthetic funding-to-session construction, HTTP replay across reopen, disconnect boundaries, ordered closure, duplicate convergence, and the bounded two-lane independent-ledger pilot. Remaining future integration tests must cover active composition ownership and authenticated transport, authenticated reconciliation evidence, multiple-generation clearing and successor activation, worker/process isolation, distributed ownership, and broader measured load and capacity behavior. No test establishes exactly-once external effects, release, or live activation.

The opt-in deadline and durable-session milestones implement durable sequencing, fencing, transactional completion adjudication, generation quarantine, advisory abort, late-settlement draining, and synthetic HTTP admission offline, but remain unreleased, unactivated, unmounted, and absent from active HTTP and composition. Authenticated transport and reconciliation, successor generations, worker/process isolation, distributed ownership, production settings, release, and live activation remain later milestones with their own threat models. Exactly-once external effects are not claimed. The active runtime remains unchanged, and the separately tracked different-operator external-evidence work remains unrelated and unchanged here.

The unreleased loopback slice is an explicitly imported transport owner for local synthetic interoperability. `createServiceCreditLoopbackServer({ handler })` expects a trusted caller-supplied handler and returns only frozen `start` and `close` operations. Passing arguments to either operation yields fixed cause-free `SERVICE_CREDIT_LOOPBACK_INVALID_INVOCATION` without changing state. It binds once to exact numeric IPv4 `127.0.0.1` and an ephemeral port; a fixed two-second startup deadline fails closed, aborts and unreferences a silent or faulty partial listener, and permanently quarantines it. Closing during startup cancels the deadline and begins one-shot shutdown immediately. The transport owner has no default or active CLI, environment selector, active-server import, or live x402 integration; the separately named basic and durable synthetic loopback demos are its only package-script callers. The returned origin and fixed path are routing metadata, not authentication. The transport is plaintext and unauthenticated and is permitted only for disposable synthetic authorization material within this local test and demo trust boundary. Any real, durable, funded, or otherwise consequential bearer material requires authenticated transport even on loopback whenever same-user processes, privileged local software, host inspection, or other same-host observers are not trusted. The generic handler cannot enforce credential provenance; its caller owns that policy.

The listener enforces fixed per-process connection, header, request-per-socket, receive-timeout, and inactivity bounds. Those limits are not coordinated across processes or hosts. Transport timeout or disconnect never claims application cancellation or accounting rollback. Close stops acceptance first and waits tracked handlers only through one short grace; a handler that remains pending or any unverifiable teardown yields fixed `SERVICE_CREDIT_LOOPBACK_CLOSE_UNCERTAIN` and permanent transport quarantine. A synchronous hung handler can block the event loop. The caller closes the SQLite store only after loopback close succeeds; uncertainty preserves the higher-level store and recovery boundary for explicit adjudication. Abrupt termination has no cleanup guarantee.

The completed mock-only activation slice captures one exact trusted verifier, authority profile/version/record digest, store, and clock in its constructor. It accepts only exact `zenon:mock` / `exact` / `upfront` funding and a full first-use `MOMENTUM_INCLUDED` result with no prior delivery. The holder must equal the signed payer. The payer-signed resource tags carry the marker and two exact halves of a domain-separated grant-funding commitment; the adapter recomputes the payment-resource digest, exact selected-requirement digest, full one-offer resource-and-requirement payment-intent, signed transaction-data, authorization, settlement, and transaction bindings. The commitment covers verifier-policy identifiers; immutable offer funding/cost-policy identifiers and versions; exact provider/service/resource/offer scope; holder and capability commitment; exact total units and expiry; scheme/flow/network/profile; and asset/amount/payee. Description text carries no authority.

The completed local composition slice captures one exact SQLite store, a descriptor-safe verifier/authority/clock options object, and an application callback behind a deeply frozen three-operation surface: `createFundingResource`, `activateFunding`, and stable-identity `handle`. It constructs and owns the mock adapter internally with that exact store, constructs admission at startup, and gives the HTTP handler only a frozen five-method execution facade. After structural validation, the exact store object is synchronously consumed before adapter or initial-handler construction and remains consumed for the loaded module instance's lifetime, including after construction failure or a latch. Recovery requires a newly opened store object and new owner, which builds its own adapter. This identity registry does not coordinate separate handles to the same file, workers, separately loaded module copies, processes, or hosts.

Outside activation attempts are serialized by a private index-based FIFO with one intrinsic async drain. It uses captured native Promise capabilities and does not call mutable Promise continuation methods or consult species. The exact adapter requires a genuine Promise from its trusted verifier and rejects a non-Promise thenable without invoking it; unavoidable native assimilation already inside that trusted adapter/Promise boundary remains in scope. Targeted Object, Set, activation-error identity, ownership, and async-context intrinsics are captured at module evaluation; this is not a general same-process hostile-code sandbox. Any activation call originating within any composition activation async context, including cross-owner reentry, fails promptly before nested dependency work; calls outside those contexts retain ordinary serialization. The owner atomically swaps later requests to a freshly validated handler only after confirmed activation. Requests racing activation retain the prior snapshot. Confirmation matches the returned activation and immutable grant bindings to the persisted embedded activation while treating the fresh handler snapshot as authoritative for mutable lifecycle and accounting counters. Thus request completion racing reconstruction does not falsely latch the owner. An exact replay may invoke the trusted verifier again but creates no new activation record, grant, or store revision; revoked or expired replay converges without authorizing the inactive grant. Definite rejection does not swap or retry.

Activation outcome uncertainty latches the owner before queued or later dependency calls and makes the stable handle return only the fixed private service-unavailable response. Confirmed commit followed by handler reconstruction failure latches the distinct `SERVICE_CREDIT_COMPOSITION_ACTIVATED_UNAVAILABLE` result without resettlement, rollback, or replacement-payment authority. A callback already running before either latch cannot be canceled; it finishes under the prior handler's existing store lifecycle. Store quarantine after outcome uncertainty makes its later durable completion uncertain and produces the fixed private unavailable response, while later requests never execute the callback. Recovery requires explicit store reopen with new exact dependency objects and new-owner construction. This owner is not imported by an active server and, outside the explicit mock demo, adds no listener, scheduler, CLI, environment activation, network, wallet, RPC, live settlement, or background recovery.

The isolated functional demo is invoked only with `npm run --silent demo:service-credit`. It performs one synthetic mock funding settlement, three unique signed provider-local service uses, and one exact replay in process. Seven units are funded and each unique use costs two; the replay does not debit or execute twice, leaving six consumed, zero held, and one available. Those provider-local uses are not additional Zenon transfers or x402 settlements. The demo constructs bounded HTTP-shaped request and response objects directly and opens no listener, socket, HTTP client, wallet, RPC, blockchain, live settlement, or live x402 path. It never retries activation or constructs a replacement funding payment after ambiguity.

The runner accepts no input and returns only one deeply frozen fixed aggregate summary. The CLI accepts no arguments and emits only fixed allowlisted success lines or one fixed failure line. Separate ephemeral mock-payment and Ed25519 capability keys use operating-system randomness; private-key bytes are never exported, capability signing-byte buffers are cleared immediately after signing, and no key, proof, authorization, transaction, identifier, response, filesystem path, or database state escapes through the public result or CLI. Production client-key generation, recovery, and lifecycle remain not implemented.

Each invocation creates a unique process-owned mode-`0700` temporary directory and one mode-`0600` plaintext SQLite ledger. Normal completion closes the store, verifies the canonical temporary parent plus captured directory and file identities, scans for protected transient encodings, unlinks only the exact ledger, removes only the verified empty directory, and confirms absence. Cleanup is deliberately non-recursive. A changed, swapped, or unexpectedly populated target is quarantined and the run fails. Abrupt termination or cleanup failure can leave a plaintext synthetic ledger, so no crash-time deletion guarantee is claimed. The filesystem boundary requires a POSIX reliable local filesystem; user-ID, mode, link-count, canonical-path, and inode checks fail closed when unavailable or inconsistent.

This is not a benchmark. It records no duration, latency, throughput, concurrency, Momentum, finality, scalability, or production-readiness result. Its purpose is only to demonstrate the already-reviewed composition and accounting semantics behind an explicit mock-only boundary.

The separately named `npm run --silent demo:service-credit-loopback` command is an opt-in, unreleased, synthetic functional demonstration of the same accounting lane over the implemented numeric-IPv4 loopback transport. Before the listener starts, it prepares one private SQLite store, one composition owner, one mock funding settlement that activates seven units, and three signed authorizations. It then performs four sequential requests in exact A, byte-identical replay A, B, C order through the existing client codec, loopback server, HTTP handler, and stable owner handle. The three unique requests execute once and consume two units each. The replay causes no second debit, execution, settlement, or revision; final accounting is six consumed, zero held, and one available. These provider-local requests are not additional Zenon transfers or x402 settlements. The existing listener-free demo remains unchanged and listener-free.

The loopback demo creates a fresh connection per attempt, follows no redirects, and makes no retry or replacement request, grant, credit, or settlement after timeout, disconnect, malformed response, or ambiguous completion. Its fixed response deadline is solely a safety bound and is not measured or reported. The deeply frozen public summary and single fixed CLI line exclude the origin, port, path, authorization, proof, public key, signature, identifiers, raw responses, database contents, timestamps, durations, and diagnostics. Plaintext unauthenticated loopback is permitted only with the demo's disposable synthetic credentials; durable, funded, externally supplied, real, or otherwise consequential bearer material requires authenticated transport whenever same-host observers are not trusted.

Normal cleanup successfully awaits loopback close before store close, verifies the exact process-owned directory and allowlisted ledger identities, unlinks only that file, and removes only the verified empty directory without recursive deletion. Close uncertainty preserves and quarantines the higher-level store and ledger rather than claiming callback cancellation, rollback, or cleanup. Abrupt termination can likewise leave the plaintext synthetic ledger. This is not a benchmark and records no duration, latency, throughput, concurrency, percentile, Momentum, finality, scalability, release, activation, or production-readiness result. It performs no wallet, RPC, blockchain, authoritative live settlement, or live x402 operation.

`activateGrantFromTrustedRecord` is the privileged model/store composition operation after verification. A raw model or store handle is grant-minting and administration authority, and the method name supplies no cryptographic inaccessibility. It must not cross an HTTP/client boundary or reach client-controlled callback code. The standalone HTTP source does not reference it and admits only already-created grants from its validated boot snapshot. The mock adapter remains the only implemented adapter that directly verifies a settlement in this repository.

The `deriveCost` callback and constructor-fixed funding-policy implementation are trusted server code whose semantics are not authenticated. The Zenon consumer binds the policy's declared immutable identity/version and its derived exact terms, while the service-credit model binds the resulting commitments; reuse of an old identity for changed code remains undetectable. Semantic changes must use a new policy identity or version and a new offer version. The provider-derived exact `totalUnits`, absolute expiry, and complete payment requirement become payer-signed through the constructed challenge; each later service request separately signs its `maxCostUnits` ceiling.

Domain state schema v2 embeds one immutable activation record in each grant. The activation and `ACTIVE` grant commit atomically in one `BEGIN IMMEDIATE` transaction, and the activation itself is the settlement-consumption marker. Exact replays converge without a revision change; changed authority/settlement, transaction, capability, activation, or grant bindings conflict before mutation. Hydration recomputes the identifiers and cross-bindings and rejects corrupt, duplicate, orphaned, inconsistent, or noncanonical records. Domain state schema v1 remains unsupported. The same one-row SQLite table now uses the separately versioned physical-v2 compound envelope described above; only the explicit guarded migrator accepts eligible physical-v1 storage that already contains valid domain state v2.

Expiry is checked before verification and again after positive evidence inside the store transaction. Positive settlement followed by expiry or a definite pre-commit failure returns `SERVICE_CREDIT_ACTIVATION_SETTLED_NOT_GRANTED` and grants no replacement-payment authority. Once commit is attempted, an exception or ambiguity returns `SERVICE_CREDIT_ACTIVATION_OUTCOME_UNKNOWN`, quarantines the store, claims neither success nor rollback, and is never automatically retried. Recovery is reopen plus explicit exact re-verification. This is one-database atomicity on one reliable local filesystem, not cross-store or multi-host coordination.

The default-inactive `src/service-credit-zenon-funding-evidence.js` contract now defines the consumer side of authoritative Zenon funding activation. It captures one synchronous deterministic/versioned provider policy, one trusted verifier, and an exact authority, SDK-safe chain profile, and confirmation policy at construction. The caller selects only an immutable offer/version, holder, capability commitment, and bound resource URL. The policy alone derives exact units, absolute expiry, payee, asset, amount, and the complete canonical requirement from the stored offer and holder/capability binding. Before construction returns that activation intent and payable challenge, one shared gate enforces every downstream activation-term constraint and requires expiry later than a valid captured clock reading. Activation re-derives and byte-compares the same terms, then retains its independent pre-verifier and post-verifier expiry checks before privileged mutation. Policy identity/version and every derived term are bound into the resource, payment intent, exact verifier record, funding commitment, and persisted activation. The funding commitment also binds evidence version 1 and authenticated-Zenon activation-record version 2.

The default-off funding-intake slice adds a separate bounded physical-schema-v1 SQLite owner before any actual HTTP ingress. It commits the exact canonical 402 frame and derived challenge before returning bytes to a caller; one exact selection has no renewal nonce. After a descriptor-only bounded snapshot, the existing offline payment preflight verifies a synthetic signed block solely against the stored challenge and binds one transaction through `ISSUED` to `BOUND` CAS. That same transaction advances the row envelope to schema v2, retains the complete exact payment payload, accepted requirement, original challenge, transaction identity and observer binding, and advances the store-wide database `user_version` from 1 to 2 on the first such BOUND. Expiry is checked before the bind as a best-effort owner gate, not atomically with the SQLite transaction. The observer target, record key and filename are persisted before attempting the independent observer-store create/open, so either crash window recovers only that target or quarantines uncertainty. Independent SQLite commits do not imply cross-store atomicity. This slice cannot observe or publish a payment, attest `READY`, activate a grant, serve an HTTP route, or establish a live testnet result.

The next separately imported publication-bridge slice owns only the BOUND-to-facilitator-journal recovery gap for one constructor-fixed selection. Construction and recovery have no publication effect. Immediately before explicit settlement, it reloads the exact retained tuple and reruns the existing cryptographic offline preflight, then invokes only the existing exact facilitator settlement method. The facilitator remains the sole journal and publication owner: it persists the same signed block before possible publication, exact-hash reconciles durable ambiguity, never republishes `SUBMISSION_ACKNOWLEDGED` or `SUBMISSION_OUTCOME_UNKNOWN`, and can retry `VALIDATED` only with the identical retained block. The bridge projects only fixed internal disposition and evidence-state values and owns no delivery, observer, attestation, activation, grant, credit, HTTP, listener, wallet, signer, RPC, WebSocket, or active composition.

The store has three independent version axes. Its physical table grammar remains v1: the exact STRICT tables and columns are unchanged, and the application identifier is also unchanged. Its database `user_version` is a store-wide rollback marker, not the table grammar or row schema: creation, pure ISSUED and legacy envelope-v1 BOUND-only state use 1, while the first envelope-v2 BOUND advances it to 2 in the same transaction. Open accepts marker 1 only without an envelope-v2 BOUND and marker 2 only with at least one; an incompatible marker/row combination fails closed without repair or mutation, and the prior marker-1-only reader rejects marker 2 at open. Independently, ISSUED and legacy BOUND rows use envelope schema v1 while each new BOUND uses envelope schema v2. Existing envelope-v1 BOUND rows remain readable for their prior local BOUND/observer behavior but are permanently ineligible for the bridge because their exact facilitator inputs cannot be reconstructed. Exact payment replay does not migrate, reinterpret, overwrite or advance the marker for them. There is no automatic migration, repair, downgrade, deletion or reinterpretation path. Rollback to the earlier reader is safe only before an envelope-v2 BOUND is committed. After that point, retain the sensitive database and compatible code; removing the bridge does not reconcile or erase either the retained payment or facilitator journal. Focused tests use synthetic in-process node methods and fake publication callbacks only. Authenticated endpoint/source policy, externally approved signer, real listener ownership, cross-database activation, chain canonicality/finality and operator review remain separate gates.

Opaque evidence is copied only after a trusted composition/ingress layer has imposed serialized/body and materialized-object size ceilings. Within that precondition the adapter applies cumulative node, member, key-byte, string-byte, canonical-byte, depth, cycle, and shared-reference limits; array length and string/key code-unit ceilings precede enumeration or byte scanning. JavaScript cannot prebound `Reflect.ownKeys` on an already materialized enormous plain object, so this is a post-ingress traversal/copy bound rather than a complete input-memory bound. The accepted verifier result remains one complete exact evidence-version-1 record.

The verifier's safe-integer `observedConfirmations` must meet the fixed threshold but is not persisted or hashed into activation identity. The domain-separated stable inclusion authorization contains `MOMENTUM_INCLUDED`, transaction hash, Momentum height and hash, and the configured satisfied-policy threshold. The same inclusion tuple at `N` and `N+k` observations converges without a revision, including after reopen; a changed height or Momentum hash for the same transaction conflicts before mutation. Definite pre-commit failure rolls back, and commit-attempt ambiguity remains reopen-and-reverify only.

Legacy synthetic mock activations retain activation-record version 1 and their exact prior shape. Authenticated Zenon records use activation-record version 2, evidence version 1, `zenontx:`, and the stable inclusion-authorization commitment. The outer domain state schema number stays 2, but the nested activation grammar has expanded. Code rollback to the earlier reader is supported only before any Zenon version-2 record is persisted. Once present, that reader fails closed; compatible code and state must be retained, with no downgrade, deletion, clearing, or reinterpretation. Malformed mixed-version shapes are rejected during both activation and hydration.

This contract is neither a concrete provider policy nor a concrete authenticated verifier. Authoritative live activation remains future integration work and must supply separately reviewed deterministic provider-policy code plus a producer that authenticates its evidence rather than relabeling caller data or the operator-trusted Gate-B record. Provider policy must return exact plain data synchronously; Promise/thenable results are invalid. The verifier's trusted return contract is an exact extensible native Promise, initially unmodified or carrying only the adapter-pinned exact constructor descriptor on replay. Unsupported Promise/thenable shapes reject, but privileged composition code that violates these contracts can still cause process effects such as an unhandled rejection; the adapter does not claim impossible observation of every hostile rejected Promise. Captured intrinsic references cover adapter-owned descriptor snapshots and the supported Promise boundary only; this is not a hostile-code sandbox and does not broaden imported-validator guarantees. Endpoint trust, canonical chain identity, reorganization handling, live composition, HTTPS deployment, release, and production persistence and operations remain unimplemented. The new module is not imported by active, HTTP, wallet, RPC, live-evidence, server, buyer, facilitator, demo, benchmark, CLI, or package-default paths and has no I/O or network effect.

The additive `src/service-credit-zenon-funding-observer-state.js` milestone supplies only the pure injected-observation state layer that a future durable observer may wrap. One state namespace fixes one transaction target and one immutable authority generation, chain profile, observer/provider/verifier policy versions, confirmation threshold, and full economic/resource/funding binding. Bounded contiguous Momentum pages advance an exact checkpoint and confirmations are derived locally. Each receipt retains only the bounded checkpoint lineage required to recompute its page digest and optional target membership; unrelated transaction membership is discarded. The first threshold record pins the actual end cursor and derived count of the first applied page or observation transition that reaches the minimum, so a multi-entry page may pin a higher count; its checkpoint equals the independently revalidated retained lineage receipt's end and threshold checkpoints. The same inclusion tuple can then strengthen monotonically without changing that record. A linked target receipt blocks both planning and direct application of a later page until the separate observation transition consumes it. Candidate projection therefore stays byte-stable after later confirmation growth. Gaps request bounded backfill without cursor movement; timeout, unavailability, and a behind source are no-ops. Cursor reproduction failure, inclusion replacement/disappearance, lineage conflict, or immutable-context drift enters terminal quarantine. Revision conflicts cannot mutate and exact replay converges. The recomputable domain commitments provide internal consistency only; an injector that controls the canonical data can recompute them, so they do not authenticate the source.

The projected `zenon-funding-observation-candidate` remains `injected-observation-only`, has no authentication or activation authority, is incompatible with `zenon-authenticated-funding-evidence`, and is rejected by the funding consumer. This slice has no filesystem, durable store, RPC/TLS/SDK source, scheduler, timer, wallet, signing, publication, HTTP route, consumer/store call, active import, deployment, release, or production behavior. It proves no endpoint trust, chain canonicality, freshness, finality, receipt, spendability, or reorganization outcome, and it does not close Issue #81. Source transport, signer custody, fencing, worker cancellation, reconnect scheduling, authenticated endpoint/checkpoint governance, and post-grant reorganization economics remain future gates. A future ingress must bound serialized and already-materialized input before calling this traversal layer. Because the SQLite owner below imports this module, remove that dependent slice first or roll both slices back together. Once no dependent remains, rollback removes exactly the pure module, focused test, and its README, security, and plan text; existing schemas and persisted state are unchanged.

The additive `src/service-credit-zenon-funding-provider-attestation.js` milestone supplies a pure, synchronous, default-inactive pinned-Ed25519 boundary. One exact canonical authority record, provisioned through an independently reviewed channel, binds the provider/key generation, network, SDK-safe chain and genesis identity, observer and confirmation policies, bootstrap checkpoint, source-policy commitment, and size/time limits. The module creates deterministic signing bytes for an exact request and verifies one exact signed envelope, but returns only the attestation identity and envelope digest. It owns no private key, signer, source, transport, state derivation, clock, store, consumer, or grant authority, and no public API can turn a caller-supplied mature observer state into a request or #104 evidence.

Its claim is only that the pinned key signed the exact request. It does not establish signer truthfulness, endpoint/node provenance, canonical chain, consensus finality, recipient receipt/spendability, or freedom from later reorganization. Plain WS, Gate-B/operator-trusted records, raw #105 candidates, transaction-only evidence, and caller flags are ineligible. Authority rotation or revocation requires a new independently provisioned profile, key, generation, and database namespace. Key custody, revocation distribution, same-identity anti-rollback, and cross-provider equivocation detection are outside v1.

The revised `src/service-credit-zenon-funding-observer-sqlite-store.js` milestone remains the narrowly scoped local persistence owner around #105, now with standalone store schema v2 and an immutable evidence outbox. The record key binds schema version, observer identity, target-binding digest, and authority-record digest. The canonical envelope retains the externally anchored authority record, immutable creation bootstrap, observer state, and exact outbox. Create accepts only a canonical pristine state reconstructed at the authority bootstrap with revision zero, `AWAITING_INCLUSION`, and null receipt, inclusion, threshold, and quarantine. Thus a mature caller state, including one that began before the bootstrap and reaches threshold exactly at it, cannot seed PREPARED. Explicit open requires the same record key and independently supplied authority anchor. The unkeyed checksum detects accidental corruption, not hostile same-identity rewriting or source authenticity.

Each page or inclusion call obtains `BEGIN IMMEDIATE`, reloads the canonical committed state, checks the exact expected revision, invokes the existing pure transition, and conditionally replaces only that envelope. The first threshold transition atomically adds one byte-stable `PREPARED` request. A valid pinned-key envelope advances the outbox to immutable `READY` and only then returns an opaque reference. Exact replay converges; a second distinct valid assertion becomes terminal `EQUIVOCATED`, while invalid input remains no-write. A committed observer quarantine atomically invalidates PREPARED or READY. Neither invalidated nor equivocated state projects candidate or evidence, although an entitlement already activated in #104 cannot be clawed back here.

`peekPreparedAttestation()` exposes only that exact committed request to a separately owned signer. `projectCommittedFundingEvidence()` returns only a digest reference, and `matchReadyFundingEvidence()` rereads and verifies the committed state, authority, request, envelope, signature, and reference before yielding the exact #104 record. The legacy `load()` method remains a privileged internal full-observer-state read but adds no public key, signing request, envelope, signature, or raw database text and must not be logged or exposed over HTTP. Observer/outbox durability and service-credit activation remain separate databases with no cross-database transaction or exactly-once claim.

All prior path, mode, link, inode, sidecar, snapshot, writer, schema, size, and commit-ambiguity controls remain. Failure at or after commit attempt closes the handle and returns no tentative state, request, artifact, evidence, or success; reopen shows the exact old or new committed state and recovery reuses the same request/envelope. The module has no source callback, RPC, SDK, timer, worker, wallet, signer/private key, transport, service-credit mutation, HTTP path, active import, deployment, release, or production behavior. It authenticates no endpoint or canonical chain and does not close Issue #81. Plain WS remains operator-trusted and cannot be upgraded by this lane.

Schema v2 has no migration, repair, downgrade, or v1 compatibility path; existing v1 databases are rejected without modification. Before any v2 database exists, rollback deletes the two new attestation paths and reverts the five modified paths in this slice. Once v2 PREPARED or READY data exists, retain compatible code and data or discard them only after proving there is no unresolved outbox state; never open or reinterpret v2 as v1. The offline `READY`-to-durable-grant, listenerless runtime, and bounded direct-HTTPS owners below fulfill their source milestones. The remaining gates are an independently audited external attestor with key custody, journal, and approval enforcement; an authenticated observation source; review and deployment of the trusted TLS/bind factory plus live transport evidence; chain, finality, reorganization, and loss policy; and operational recovery. No additional generic infrastructure milestone is scheduled first.

The additive `src/service-credit-zenon-funding-composition.js` milestone implements the offline consumer half of that bounded composition. It requires exact unadorned instances of the observer/outbox and service-credit SQLite stores, exercises captured read-only methods during construction to prove their private brands, captures their privileged prototype operations, pins one canonical authority record, recomputes the authority-bound observer record key, and hides the authenticated-Zenon consumer behind fixed provider-policy and verifier closures. A challenge request rereads the committed observer before and after provider-policy evaluation and revalidates the authority, record key, and all captured immutable bindings. It returns a provider-derived payable resource only for an open, non-quarantined `NONE`, `PREPARED`, or `READY` outbox whose complete binding matches the immutable observer target; `INVALIDATED`, `EQUIVOCATED`, corrupt, closed, replaced, or drifted observer state returns no challenge. The caller can later ask to activate the store's exact committed `READY` reference, but cannot supply or replace evidence, authority, key, observer state, verifier, matcher, store, outbox, or grant authority. A second committed-state match occurs before grant mutation, so intervening invalidation or valid equivocation fails closed.

Only one activation may be in flight; exact concurrent invocation shares it, a different input is rejected without a queue, and exact completed replay converges on the existing activation and grant. A service-credit commit acknowledgement ambiguity returns no success and permanently latches the owner. Recovery closes and reopens both stores, reconstructs the owner with the same fixed bindings, and replays the same immutable `READY` reference. The observer outbox is not marked consumed. Because each database commits independently, this milestone adds no cross-database transaction, exactly-once consumption, or anti-rollback property.

The assertion means only that the pinned provider key signed the exact historical observation at the configured threshold. It does not prove signer truthfulness, endpoint provenance, canonical chain or finality, recipient receipt or spendability, or freedom from later reorganization. Plain WS and Gate-B remain operator-trusted and cannot satisfy this lane. Direct listener-free integration tests exercise activation, the durable HTTP session, exact response replay, and reopen accounting, but the module adds no signer, private key, source, RPC/WebSocket client, wallet/payment operation, listener, route, active import, deployment, release, or live behavior. The bounded default-off runtime and direct-HTTPS source owners are implemented below; the external producer, attestor implementation, key custody and lifecycle, reviewed TLS/bind factory and live transport evidence, and operational policy remain separate gates. Issue #81 remains open and unrelated.

No schema changes in this slice. Rollback removes the composition source, focused test, narrow architecture assertion, and these three documentation updates, but cannot erase an already committed observer `READY` record or undo an activated service-credit grant. Any ambiguous activation must be reconciled by reopen and exact replay first; compatible readers remain required until retained database state is separately proven safe to discard.

This additive milestone, `src/service-credit-zenon-durable-http-composition.js`, implements the default-off and unmounted lifecycle around that offline composition. It owns exactly one `NEW -> STARTING -> ACTIVE -> CLOSING -> CLOSED` sequence plus conservative `PRE_ACTIVATION_UNAVAILABLE`, `RECOVERY_REQUIRED`, and `ACTIVATED_UNAVAILABLE` failures. Construction requires exact open stores and a committed `READY` observer, captures fixed authority, funding policy, clock, durable-execution configuration, application execution, and deadline capabilities, validates configuration with the existing pure execution contract, and performs bounded reads only. It creates no file, timer, listener, transport, source, signer, wallet action, payment, RPC, WebSocket, or network effect.

Startup initializes or exactly reuses one matching empty/open execution generation before grant activation because this is the only exact check against the store's private request-capacity limit. That deterministic preinitialization grants no service, may remain after a later definite activation rejection, and is safely reused on exact replay. Startup then privately replays the committed assertion into durable activation, constructs the listener-free durable HTTP session, and publishes `ACTIVE` only after the session is usable. Identical concurrent calls share one native Promise; changed calls conflict and are not queued. Every other lifecycle state serves independently allocated fixed private unavailable bytes without application execution. Definite pre-activation failure does not claim an activated grant. Activation or initialization commit ambiguity remains `RECOVERY_REQUIRED`; definite failure after activation remains `ACTIVATED_UNAVAILABLE`.

Close stops new admission first, waits for controllable startup and admitted work, closes the session, then closes observer and service stores in fixed order. Concurrent or prior `RECOVERY_REQUIRED` and `ACTIVATED_UNAVAILABLE` outcomes remain terminal after safe closure, and repeated close returns the same fixed failure rather than implying clean reconciliation. It assumes exclusive logical custody only among cooperating same-process code; code retaining raw handles remains trusted. The bounded HTTPS owner must stop external ingress and close its downstream router before this close. The immutable `READY` outbox is never consumed, databases remain independent, and no cross-database transaction, exactly-once, or anti-rollback property is claimed. Post-activation invalidation cannot claw back consumed service credit.

The internal caller must prebound serialized and already-materialized input size. Traversal ceilings bound copying after ingress but cannot prebound the memory already allocated for an arbitrarily large plain object's own-key set.

This is an offline provider-authenticated assertion composition, not deployment or live-product readiness. The signature does not prove provider truthfulness, authenticated node provenance, canonical chain, consensus finality, recipient receipt or spendability, or reorganization immunity. Plain WS remains operator-trusted and cannot enter this lane. Issue #81 remains open and separate. The remaining gates are an independently provisioned authenticated producer/signer/key-custody lifecycle, review and deployment of the trusted TLS/bind factory plus live authenticated-transport evidence, chain/finality/reorganization/loss policy, and operational recovery.

This slice changes no schema. Before use, rollback removes its source, focused test, narrow architecture assertion, and these documentation paragraphs. After use, stop admission, prove clean owner/session closure, reconcile ambiguous activation by reopening both stores and replaying the exact `READY` reference, and retain compatible observer, activation, execution, and journal data. Removing the code does not revoke, reconcile, or downgrade committed state.

The additive milestone, `src/service-credit-zenon-provider-signing-child-protocol.js`, defines one canonical version-1 frame in each direction over two dedicated inherited descriptors. A four-byte big-endian length prefix bounds one exact canonical UTF-8 JSON payload; the peer must then close the direction and no trailing or second frame is accepted. The request contains the store-issued PREPARED request and a stable domain-separated operation identity over protocol version, authority-record digest, authority-generation commitment, key identifier, and attestation identity. It excludes attempt number, process identity, descriptor numbers, endpoint, path, timing, and caller approval. The child-side parser requires the independently pinned authority and reuses the existing complete attestation-request grammar. Response status is exactly `APPROVAL_REQUIRED`, deterministic `REJECTED`, or `READY` with one canonical envelope.

`src/service-credit-zenon-provider-signing-operation.js` owns exactly one possible external-child dispatch. Construction captures a genuine open observer store, canonical authority, fixed absolute executable path and digest, current effective-user identity, explicit clock, deadline controls, and response limits without spawning or scheduling; absence of a valid effective-user identity is a configuration failure. `start()` requires that identity to remain unchanged, reloads committed PREPARED, recomputes the authority and record binding, derives the stable operation identity, and validates every canonical path component from the filesystem root through the executable. Ancestors must be stable root- or effective-user-owned directories without group/world write permission, except for the exact root-owned sticky-directory case; the executable must remain a restrictive regular non-symlink with the pinned bounded SHA-256 identity. Immediately after spawn returns, persistent captured handlers arm the child and both dedicated pipes before post-spawn inspection or request framing. Request dispatch remains impossible until those handlers, the repeated executable check, and the repeated PREPARED check succeed. It then sends one frame with empty arguments, a configured empty environment, no shell or PATH lookup, ignored standard streams, and only the request and response pipes at the dedicated descriptor positions. The complete component and executable identity is checked immediately before spawn, immediately after spawn, and again after response. The repository supplies no external implementation that signs or owns a key.

The owner publishes one native in-flight Promise before any caller-controlled deadline or spawn runtime can reenter. It freshly reloads and compares the exact committed PREPARED request before executable inspection, immediately before and after spawn, after response collection, and—following the injected clock and cryptographic checks—immediately before either expiry classification or import. Child, request-pipe, and response-pipe handlers remain armed through cleanup and conservatively classify write completion, early closure, partial failure, exit/error ordering, kill failure, and late or duplicate events. A proven failure before request dispatch is definite only when no request byte could have been written. Any timeout, crash, missing or malformed response, signature rejection, or executable, channel, or PREPARED uncertainty once a write may have begun becomes terminal `SIGNER_OUTCOME_UNKNOWN`; a provable committed observer terminal state may produce its stronger fixed result, and store commit uncertainty becomes `STORE_RECOVERY_REQUIRED`. It returns `READY_COMMITTED` only after the existing Ed25519 verifier accepts the envelope and the existing observer store durably commits and postchecks READY. Neither uncertainty permits retry or replacement. `APPROVAL_REQUIRED`, deterministic rejection, committed invalidation, valid equivocation, and `RECOVERY_WINDOW_EXPIRED` are likewise terminal for one owner. Exact concurrent starts share that Promise, repeated terminal calls do not spawn again, and close preserves unresolved classifications.

The external provider attestor remains a privileged deployment component. It must independently enforce one human approval per attestation and durably journal stable operation identity to exactly one immutable envelope so reconstruction after ambiguity receives the same envelope. This prevents authorization of an alternate replacement envelope but cannot guarantee import liveness: #107's existing INITIAL initial-age, future-skew, and validity policy still applies. After the injected clock and cryptographic checks, the client performs one final exact PREPARED reload. An identical cryptographically valid envelope replayed after either import window has elapsed commits nothing, retains PREPARED and external journal evidence, requests no replacement, and terminates as `RECOVERY_WINDOW_EXPIRED` only if that reload still proves PREPARED unchanged; otherwise the stronger store/drift classification wins. A future durable dispatch-authorization schema or explicit reviewed rollover is separate work. This repository-side client cannot verify the journal or approval event and accepts no caller-provided `approved` signal. Key generation, private-key custody, HSM/keystore access, attestor update policy, key rotation/revocation, and external anti-rollback remain unimplemented. Repeated component/path/digest checks reduce accidental drift but do not exclude same-effective-UID/root interference, transitive executable dependencies, or the unavoidable path-to-exec race.

The resulting assertion remains only provider-authenticated history under the pinned key and reviewed `sourcePolicyCommitment`. It does not prove provider truthfulness, endpoint or node provenance, public canonicality, finality, recipient receipt, spendability, or reorganization immunity. Plain WS remains operator-trusted and cannot enter this path. This milestone changes no schema and is absent from active package/server/CLI/runtime imports. Before use it rolls back as exactly eight paths. After possible child dispatch or READY import, rollback first requires signer/store reconciliation while retaining the observer database and external journal; removing the client is neither recovery nor revocation.

The separate `native/provider-attestor/` development source now supplies a journaled one-operation child, PKCS#11 signer-policy draft, and macOS approval/Keychain adapter. It is not provisioned or deployed: the release-only entrypoint needs separately supplied compile-time pins, test pins and fake adapters are isolated, and the signer-loader gate unconditionally denies loading and signing. No ordinary runtime package script or active Node import graph starts it. Local offline checks cover framing, synthetic journal/replay, fake LocalAuthentication/Keychain, loader-policy fixtures, and disposable-token SoftHSM 2.7.0 exact key-profile behavior. They do not qualify a real prompt or Keychain item, installed-image/dependency identity, or release operation; caller-owned credential wiping does not prove zeroization of the framework-owned Keychain return buffer. Reviewed pins, key custody, loader/platform qualification, authenticated observations, independent chain verification, and recovery/anti-rollback remain gates. This source establishes no independent chain truth, testnet signing, payment, live activation, release, or production readiness.

The provider is trusted for service accounting, availability, refunds, and unused value. Without enforceable redemption or escrow, this design must not be described as trustless, a payment channel, or official x402 batch settlement.

The implemented service-credit ledger store uses the built-in `node:sqlite` API on the Node.js 24+ package baseline, remains confined to one reliable local filesystem, and stores only reviewed bounded facts and commitments in plaintext. Raw payment headers, signed blocks or signatures, keys beyond the one-way capability commitment, bearer proofs or preimages, response bodies, personal data, arbitrary verifier output, and secrets are excluded from that ledger. This is distinct from the observer/outbox schema-v2 store above, which intentionally retains its canonical public authority record, PREPARED signing request, and READY signed envelope under its separate privileged boundary. Node.js 24 documents this API as Stability 1.2, release candidate. The explicit mock, basic loopback, and durable loopback demos import the offline lane; no active runtime does. The basic loopback-demo milestone can be rolled back by removing `src/service-credit-loopback-demo.js`, `src/service-credit-loopback-demo-cli.js`, `test/service-credit-loopback-demo.test.js`, its package script, and its text from `README.md`, `SECURITY.md`, and this plan, while reverting only the corresponding milestone assertions in `test/service-credit-loopback-server.test.js`. That exact eight-path rollback leaves the listener-free and durable demos, transport owner, active runtime, and persisted schema unchanged. The earlier transport-owner milestone remains independently reversible by removing only `src/service-credit-loopback-server.js`, its focused test, and its documentation. Any retained state-v2 database must still be preserved and opened only by a schema-v2 reader during rollback.

This loopback demo makes no production, live-payment, authoritative-settlement, wallet, RPC, blockchain, x402 finality, performance, latency, throughput, concurrency, percentile, Momentum, scalability, or release claim. Any benchmark requires a separately named, separately reviewed future milestone; authenticated transport and consequential credential use also remain future work.

The authenticated future transport requirement remains mandatory for every non-synthetic or consequential credential use; the narrow local plaintext exception above does not weaken redaction.

The separately named `npm run --silent benchmark:service-credit-local` command is an opt-in local synthetic scenario benchmark over the listener-free and basic loopback demo runners; it does not invoke the durable loopback demo. In one warm Node.js 24 process it performs 35 paired rounds per lane, treating rounds 0 through 4 as warmup and rounds 5 through 34 as 30 measured samples. Even rounds execute listener-free then loopback; odd rounds reverse that order, giving each lane 15 measured first-position runs. Calls are strictly sequential with concurrency one. A synchronous module-local latch rejects concurrent or reentrant use of one loaded benchmark module instance and is released after success or failure; it is not cross-worker, cross-process, or global authority. Each monotonic sample covers one whole demo invocation, including its cleanup, and ends before benchmark-side fixed-summary validation.

Before any clock or runner call, the gate verifies only Node major 24, an empty live `execArgv`, and empty or absent `NODE_OPTIONS`, `NODE_V8_COVERAGE`, `NODE_DEBUG`, and `NODE_DEBUG_NATIVE`. The command should run in an operator-controlled fresh process. The gate cannot attest the absence of external instrumentation, trusted same-process mutation or module replacement, workers, or operating-system observation. The runner then uses only `process.hrtime.bigint()`, keeps raw samples as integer nanoseconds, and reports half-up rounded median and nearest-rank p95 microseconds for each lane. It uses no trimming, outlier removal, retry, replacement, parallel call, `Promise.race`, or general timeout. It has no threshold. A separately designed cancellation boundary is required before any deadline can safely claim that timed-out work stopped. Any runner, clock, summary, conversion, serialization, or output ambiguity fails with one cause-free status and no complete benchmark result.

Results are host-, filesystem-, and runtime-specific descriptive observations. The two lanes are structurally different scenarios, so their difference is not isolated loopback overhead and is not a universal comparison. Both use the current invocation's POSIX local temporary filesystem and synchronous SQLite durability. Median and p95 are the only variable timing measurements. Fixed allowlisted schema metadata also reports the Node major, clock identity, measurement target, warmup and measured counts, concurrency, order, duration unit, rounding method, and percentile method. The compact JSON omits detailed host and process identifiers, environment values, paths, ports, origins, credential identifiers, credential material, raw samples, and diagnostics, but aggregate timing can still characterize host performance. The benchmark does not persist or commit results. It does not upload results, and the standard test suite uses only fake runners rather than executing the real 70-run workload.

Interruption, underlying cleanup failure or quarantine, and loopback-close uncertainty inherit the existing demos' possible plaintext synthetic-ledger residue. The CLI attempts one synchronous stdout write only after complete success and one fixed stderr write after failure. A failed or short descriptor write may already have emitted an allowlisted partial prefix; stdout and stderr from every nonzero run are invalid and must be discarded. This is not a production, per-request latency, TPS, throughput, concurrency-capacity, network or payment latency, L1-capacity, Momentum, finality, live-payment, release, activation, scalability, or production-readiness measurement. The benchmark does not change authentication policy: disposable synthetic loopback credentials retain the narrow local exception, while consequential material requires authenticated transport.

Rollback is exactly seven paths: remove `src/service-credit-local-benchmark.js`, `src/service-credit-local-benchmark-cli.js`, and `test/service-credit-local-benchmark.test.js`; remove `benchmark:service-credit-local` from `package.json`; and remove only this milestone's text from `README.md`, `SECURITY.md`, and `docs/IMPLEMENTATION_PLAN.md`. That rollback leaves both demos, the codec, HTTP handler, loopback transport, composition owner, store, schema, active runtime, dependencies, and lockfile unchanged.

## Frontier architecture

`prepareBlock()` fixes `height` and `previousHash`. Two payments prepared from one payer frontier can conflict.

### Current mitigation: process-local ordering

The facilitator reconciles a previously known transaction before applying a new frontier-sensitive publication check. The canonical per-payer queue orders one facilitator instance, while the global SDK owner serializes live sessions across instances in the process. Bounded unconfirmed inspection rejects a conflicting node-observed block, but cannot make the node view atomic.

This cannot serialize buyer-side preparation, another process, another facilitator, a stale node view or an independent publisher.

### Near-term option: payment subaccounts

Agents can derive multiple payment accounts and serialize within each one. This uses existing protocol behavior but requires liquidity and Plasma management.

### Long-term research

A native sequence-independent authorization or escrow/voucher scheme could remove frontier coupling. Neither is an existing Zenon primitive assumed by this prototype, and each requires separate protocol design and security review.

## Plasma and signing

`prepareBlock()` can perform PoW when fused Plasma is insufficient. Production agent UX may need pre-fused payment accounts, provisioned subaccounts or an explicitly designed sponsorship mechanism.

The buyer or approval UI should display the actual account block being signed: recipient, concrete ZTS, amount, chain profile, account frontier, intent/resource digest and any Plasma/PoW fields. It should not authorize only the server-provided prose.

## First-class x402 package target

An upstream package should implement the official client, server and facilitator interfaces rather than maintaining custom core HTTP helpers:

```text
packages/mechanisms/zenon/
  src/
    exact/
      client/scheme.ts
      server/scheme.ts
      facilitator/scheme.ts
    types.ts
    signer.ts
    index.ts
```

Zenon-specific address, ZTS, block-hash, signature, frontier, Plasma/PoW, publication and observation policy belongs in that package, not x402 core.

Before publishing network defaults, establish canonical namespace/reference naming and immutable profile metadata. The label alone must never substitute for chain identity.

## Production requirements

A production facilitator still needs:

- an authenticated chain trust root and independently verified inclusion policy;
- explicit client sessions rather than a mutable SDK singleton, or a Go service with clear lifecycle ownership;
- cancellable RPC with deadlines below the transport;
- an integrated production-grade entitlement store shared by every service instance, plus distributed coordination; the single-host SQLite reference does not satisfy this requirement;
- durable entitlement/delivery design appropriate to each resource side effect;
- rate limiting, RPC failover and operational telemetry without wallet secrets;
- an explicit confirmation-depth and reorganization policy;
- merchant receive-account-block handling where the business requires it;
- an audited payment-intent encoding and x402 conformance suite;
- client-key provisioning, authentication, rotation, revocation and recovery without persisting private keys or bearer secrets;
- explicit expiry, refund, redemption and unused-balance terms visible to the payer.

## Test roadmap

The local suite covers strict wire shapes, amount/profile boundaries, hash/signature reconstruction, offline preflight, singleton ownership, poisoning, journal corruption/reload, state monotonicity, pagination, retries, duplicate delivery and ambiguous HTTP outcomes.

The offline service-credit tests additionally cover immutable offers, activations, and grants; every durable activation binding; exact mock settlement-to-grant activation and signed funding tags; asserted, partial, malformed, unconfirmed, expired, mismatched, and holder/payer evidence rejection; exact accounting; signed maximum cost; pinned request cost; replay; expiry; revocation; conservative ambiguity; state hydration; state-v1 rejection; SQLite corruption and path rejection; commit uncertainty; restart recovery; same-process and local cross-process exact/conflicting activation races; bounded capacity; Ed25519 proof binding and rejection; canonical client authorization serialization and hostile-shape rejection; exact HTTP routing and framing; fixed responses; boot-time grant admission; per-handler admission limits; concurrent replay convergence; callback ambiguity; exact/frozen composition authority; exact-object ownership and failed-construction consumption; prior-snapshot and replay/counter activation races; serialized activation under Promise mutation; same-owner and cross-owner verifier reentry; definite verifier failures without swap; inactive replay with unrelated active grants; in-flight callback completion at a latch; owner-wide outcome-unknown and activated-unavailable latches; fixed unavailable HTTP behavior; explicit new-owner restart recovery; opt-in runtime deadline scheduling; transactional completion adjudication; fixed advisory-abort ordering and reason; late-settlement draining; terminal races; wait-only close; synthetic durable HTTP session construction; mock funding, replay, reopen, disconnect, ambiguity, and duplicate-convergence integration; deterministic listener-free, basic loopback, and durable two-phase loopback demo output; two same-process independent-ledger pilot lanes with drain-all failure handling; sequential no-retry transport behavior; close-before-store ordering; close-uncertainty quarantine; phase-boundary replacement/symlink/hard-link/sidecar rejection; normal and representative-failure cleanup; swapped-directory quarantine; import-closure isolation; and plaintext-ledger secret-field exclusion. Future integration tests must cover authoritative live settlement activation, client-key lifecycle, authenticated transport and log redaction, general response persistence, active HTTP/composition propagation of the reviewed advisory-signal policy, deliberate successor-generation migration and downgrade policy, distributed ownership, reconciliation authorization, and broader measured load and capacity behavior before any production claim.

Future isolated devnet tests should cover protected operator material, local-only exposure, fresh accounts, Plasma and PoW, sequential and conflicting preparations, process restart at every journal transition, delayed inclusion, node disconnect/reconnect and merchant receive. The committed offline artifact boundary must remain separate from the public-testnet policy, and no live-network integration test should run by default.
